# 《择决千秋》统一剧情框架 · 叙事设计文档

> 角色：NarrativeDesigner（叙事系统架构师）
> 目标：把"各月选择分散、连贯性不强"的诊断，转译为一套**可直接挂接现有代码**的统一剧情系统。
> 兼容性前提：所有新增内容均为**可选字段 / 提示词增量**，不改动六维、不改动 `enterMonth`/`makeChoice`/`endGame` 主流程，对已入库的 1866 则事件零回归。
> 依据代码基线：`index.html`（六维、`PROMPTS`、`enterMonth`、`pickGatedMajor`、`computePressure`、`state.flags`/`relations`、`ENDING_RULES`、`CANONICAL_FLAGS`、`FAIL_DIMS`）、`data/emperors/*.json`（事件 schema）。

---

## 〇、根因诊断：为什么各月"散"

直接对应代码，散的根源有四处，且**都与现有机制不冲突，只是机制没被叙事层使用**：

| # | 散的表现 | 代码根因 | 现有可用机制（没被用起来） |
|---|----------|----------|----------------------------|
| 0-1 | 月度事件像一个个独立小品 | `PROMPTS.user`（L1923）每月只注入「近 5 月标题 + 数值 + 锚点 + 关系」，**缺主线索 / 幕次 / 活跃旗标 / 具名人物** | `emperor.eraContext`、`state.year/progress`、`state.flags`、`state.relations` 全在内存里，只是没喂给 AI |
| 0-2 | "选择有重量"在月度层是假的 | `state.flags` 被置位（L3012/L3044），但**从不进 AI 提示词**；月度事件不引用已发生的抉择 | `CANONICAL_FLAGS` + `gateOK`（L2587）已支持旗标门控，但预设事件几乎没写 `branches` |
| 0-3 | 没有"一直跟着我的人" | 人物只有 `宗室/士大夫/边将/商贾` 4 个汇总桶（L2067），无具名常驻角色 | `state.relations` 是任意对象，可挂具名键 |
| 0-4 | 史官点评是装饰 | `historianComment` 只概括本月（L3070 模板），**不回指统治弧线 / 不提示后续** | `recentSummary` 已存在，可扩展为"弧线指针" |

**一句话**：机制骨架已经能撑住统一剧情，缺的是**叙事层把骨架用起来**。下面给出的就是这层"用法"。

---

## 一、统一剧情总设计：三层统一

统一不是"加一条主线剧情"，而是让**三种连续性**同时生效：

```
┌─────────────────────────────────────────────┐
│  声线统一：史官（跨 262 帝同一支笔）          │  ← 最廉价、最立竿见影
├─────────────────────────────────────────────┤
│  线索统一：每帝一条"主线索" + 四幕统治弧      │  ← 给每月事件一个"为什么是现在"的由头
├─────────────────────────────────────────────┤
│  人物统一：常驻班底（具名、跨月持续关系）     │  ← 让玩家"认识人"，而非只认识数值
└─────────────────────────────────────────────┘
         ↑ 三者全部通过"AI 提示词注入"落地，
           无需改写 1866 则事件，只改 PROMPTS + 加可选数据字段。
```

### 1.1 每帝一条"主线索"（The Spine）

主线索 = **一句中央戏剧问题 + 四幕张力**。它在 `emperor` 数据里是可选字段 `reignPremise`，由叙事编写者撰写，每月注入 AI。

> **主线索写法公式**：`[出身/起点] 之身，能否以 [手段] 成就 [野心]？其代价 [张力] 将如何反噬？`
>
> - 努尔哈赤：*"边徼部族之主，能否以武力立国、突破中原千年之防？然麾下诸部离心、汉民未附，穷兵则基业不稳。"*
> - 唐太宗：*"杀兄逼父而得天下，能否以仁政洗刷原罪、成贞观之治？然元勋骄横、突厥未灭，功高者难制。"*
> - 宋高宗：*"半壁江山之主，偏安与复仇孰重？父兄之仇、臣民之望、权将之逼，三者不可兼得。"*

中央戏剧问题就是每月事件的"引力源"——AI 拿到它，编出的事件自然围绕同一矛盾旋转，散感消失。

### 1.2 四幕统治弧（每帝通用骨架）

四幕不是新机制，是**给现有 `computePressure` 高斯峰和 `midCrisisAnchors` 一个叙事解释**：

| 幕 | 进度 | 系统钩子 | 叙事任务 | AI 事件基调 |
|----|------|----------|----------|-------------|
| **第一幕 定鼎** | 0–25% | `pressure` 谷底、危机极少 | 立威、安内、确立主线索 | 奠基类：用人、立制、服众 |
| **第二幕 守成·拓业** | 25–55% | `pressure` 缓升 | 把主线索推向行动（征/改/抚） | 经营类：扩张、新政、外交 |
| **第三幕 中局大考** | 55–80% | **`pressure` 高斯峰**（L2570 `bell` 峰值在 0.5）+ `midCrisisAnchors` 定时炸弹落地 | 主线索的反噬到来（兵变/清议/民变/边叛） | 危机类：代价兑现、抉择分水岭 |
| **第四幕 晚岁·遗烈** | 80–100%+ | `pressure` 缓降但 `health` 自然衰减（`reignEnd+5` 后 −3） | 收束：传位/盖棺/遗留隐患 | legacy 类：继承、评功、身后事 |

> **关键洞察**：第三幕"大考"**已经由代码天然提供**（高斯峰 + 定时炸弹），我们只是给它一个叙事身份——"你早年种下的因，现在结果了"。这无需新增任何难度代码，纯叙事标注。

---

## 二、史官叙事层（统一声线）—— Voice Pillars

史官是**唯一贯穿所有皇帝、所有月份的声音**，是统一感的最大杠杆。当前史官点评是模板化 flavour（L3070），我们把它升级为**有固定声线的叙事者**。

### 2.1 史官（跨帝统一声线）Voice Pillars

```markdown
## 角色：史官（ narrator / 史臣秉笔）

### Identity
- 角色定位：以《资治通鉴》"臣光曰"为体的拟史官，代玩家身后立言
- Core Wound：亲见太多雄主毁于自用，故评赞中总藏一句隐忧
- Desire：为后世留下"劝惩"之鉴
- Need：在颂圣与直笔之间取得平衡（不谄不苛）

### Voice Pillars
- Vocabulary：文言/半文言；好用"帝""上""竟""然""惜乎""论者谓"
- Sentence Rhythm：短句起势 + 四字评断收尾（"帝从之，边备稍弛。论者谓：务远略而忘近忧。"）
- Topics They Avoid：不剧透未来、不评价玩家"对错"、不跳出史书体例
- Verbal Tics："——然…""惜乎""其后验之""论者谓"
- Subtext Default：永远在"记一事"与"埋一线"之间——点评末尾那半句，是对主线索的回指

### 史官评的三重功能（改造要点）
1. 记：概括本月事件后果（现有）
2. 指：末尾半句回指主线索进度（新增）——"此征伐之始，亦四境多事之端"
3. 悬：对旗标状态轻轻点出"未了之事"（新增）——"变法之议既行，清议之波方兴"

### 每帝声线变体（轻量）
通过 emperor.eraContext + 一个可选 voiceTag 微调：
- voiceTag:"质朴"（汉初/五代）→ 少用典故，直叙
- voiceTag:"华赡"（唐/宋/清）→ 可引经据典
- voiceTag:"冷峻"（亡国之主多）→ 删"惜乎"类软词
```

### 2.2 史官评从 flavour → 弧线指针（代码示例）

把 `stanceCommentText`（L3069）从纯模板，改为**读取当前幕与活跃旗标**生成：

```
帝纳「{choice}」之策。{本幕基调评} {主线索回指半句}
例（第三幕·reform 旗标活跃）："帝锐志更化，朝议汹汹。论者谓：变法之弓已满，惜乎清议之弦将断。"
```

实现：在 `renderOutcome` 调用处，传入 `state.flags` 与当前幕，史官评模板按"活跃旗标 → 对应悬语"查表（见第五节旗标表）。**纯增量，不改数值**。

### 2.3 已落地实现（代码级，P1 完成）

史官声线已从"规划"转为 `index.html` 内的软约束，且**不依赖改写 1866 则事件、不改六维主流程**：

- **史官口吻约束**：新增 `HISTORIAN_VOICE`（通用拟史官体：文言/半文言、臣光曰体、记—指—悬三段式、避讳清单、篇幅）与 `VOICE_TAG_VARIANT`（质朴/华赡/冷峻 三档轻量变体）。`historianVoiceFor(emp)` 在 `PROMPTS.system` 的【本朝叙事主线索】注入，作为 AI 生成"事件描述 + 史官评"的贯穿声线软约束。
- **每月批注（historianComment）升级**：在 `PROMPTS` 的 outcome / 预设 / 日常三处提示词里，把史官评要求从"史官评(30-60字)"强化为"记—指—悬三段式"——① 记本月后果；② 指——末半句回指主线索进展；③ 悬——对活跃旗标轻轻点出未了之事。**仍由 AI 生成**，保留文字生成能力与叙事随机性（契合"不要全部固定"的要求）。兜底路径 `stanceCommentText` 维持简短史官体。
- **三类话术骨架（贯穿全程）**：
  1. **登基定场白** `historianPrologueFor(emp)`：由 `reignPremise` 提取核心问句，生成"史臣曰：…此其一生之问也。"（`voiceTag:"冷峻"` 时自动改"史臣直书"）。`selectEmperor` 注入 intro 屏新增的 `#intro-verdict`（无 spine 则 `.hide`，零回归）。
  2. **每月史官评**：见上，AI 生成、记—指—悬。
  3. **结局判词** `historianEpilogueFor(emp, ending)`：引用主线索问句 + 按结局 `id`/`title` 映射"应答短语"（unify→竟成混一；明君→得善答；亡国之主→遂湮…）。`endGame` 追加至 `#end-review`（该元素已加 `white-space:pre-line` 以支持换行），无 spine 则保持原 `review`。

> **零回归保证**：`emperor` 缺 `reignPremise`/`voiceTag`/`cast` 时，上述注入全部跳过——`system` 不输出史官口吻块、`selectEmperor` 隐藏定场白、`endGame` 保持原 review，旧皇帝与未铺 spine的 259 帝行为完全不变。

### 2.4 P3 铺陈：史官线覆盖全部 262 帝（已落地）

**目标**：让"史官这条线"在 262 帝里普遍生效，而非仅 3 位样板帝。

**策略（守"不固定、保随机"）**：
- **Tier A（73 帝）**：手写精修 `reignPremise`（一句中央戏剧问题，带主线索张力）+ `voiceTag`（质朴/华赡/冷峻）+ `cast`（2-4 位真实班底，含 role 与 voiceTag）。精修表分 `_spine_a.js`（夏/商/汉/晋/南朝/北魏北周/隋/唐除太宗高宗/五代/辽/金/西夏/元/清太宗）与 `_spine_song_ming.js`（宋 Tier A 余帝 + 明仁宗/宣宗/孝宗）。
- **Tier B（160 帝）**：模板化 `reignPremise`（按朝代生成典型矛盾问句）+ `voiceTag` + 朝代典型班底池（汉/唐/宋/明/元/清各配 1 个代表班底）。
- **Tier C（29 帝）**：模板化 `reignPremise` + `voiceTag`，班底省略。
- **3 位既有手写精修帝**（qing_taizu/tang_taizong/song_gaozong）：注入脚本检测 `reignPremise` 已存在即**跳过不覆盖**，保留人工精修版。

**合并脚本**：`tools/_inject_spine.js` 安全合并进 `data/emperors/*.json`（仅新增三字段，不触既有事件）；含 `data/emperors/_backup_20260807/` 备份。

**落地统计（重建后 `coh_data.js` 校验）**：
| 类别 | 数量 | 说明 |
|---|---|---|
| 已铺 spine 总计 | **262 / 262** | 全覆盖，零遗漏 |
| 保留手写精修 | 3 | 努尔哈赤/唐太宗/宋高宗 |
| 手写精修（脚本注入）| 69 | Tier A 其余名帝 |
| 模板化（Tier B/C）| 190 | 按朝代生成 |
| 配 cast 班底 | 114 | 73 精修 + 41 Tier B 典型 |

**验证**：全文件内联 JS `node --check` 通过；`historianPrologueFor`/`historianEpilogueFor`/`historianVoiceFor` 对精修帝（玄宗"开元之治、天宝之乱"、徽宗"瘦金之笔不误乾坤"、刘备"织席贩履续汉室"）与模板帝均输出正确；修复了 `index.html` 中 2 处残留的 git 冲突标记（`<<<<<<<`/`=======`/`>>>>>>>`），合并时保留了对方的越代科技提示、时间一致性、`abdicated/restored` 词表等改进，并补回史官"记—指—悬三段式"要求。

---

## 三、常驻人物体系（角色连贯性）

### 3.1 数据模型（兼容现有 `relations`）

在 `emperor` 增加可选 `cast` 数组，**具名角色映射到既有 4 阵营桶**做数值，但 AI 用其"名字+关系"制造连贯：

```jsonc
"cast": [
  { "id":"eyidu",  "name":"额亦都", "role":"宿将", "faction":"边将", "init":42,
    "voiceTag":"悍直", "arc":"从微时伴驾到晚岁骄倨" },
  { "id":"shuerhaci","name":"舒尔哈齐", "role":"宗亲·弟", "faction":"宗室", "init":55,
    "voiceTag":"郁勃", "arc":"功高见忌，终至失和" },
  { "id":"gongzhenglu","name":"龚正陆", "role":"谋主·汉儒", "faction":"士大夫", "init":20,
    "voiceTag":"谨婉", "arc":"倡抚汉民、行制典，不见信于贵胄" }
]
```

运行期：`newState` 把 `cast[].init` 并入 `state.relations`（具名键 + 阵营键并存），`relationDeltas` 可同时动"边将"桶与"eyidu"具名键。

### 3.2 五类原型 Voice Pillars（让人物"像人"）

| 原型 | 词汇 | 节奏 | 避谈 | 口头禅 |
|------|------|------|------|--------|
| 谋主 | 援引经史、言"宜""当" | 长句铺陈利弊 | 不直斥君主 | "臣窃以为…" |
| 宿将 | 简硬、多"某""愿" | 短促斩截 | 不谈文治 | "末将请战" |
| 谏臣 | 引祖制、称"不可" | 先扬后抑 | 不谄 | "陛下三思" |
| 宗亲 | 称"吾辈""家" | 亲昵中带试探 | 不揭短 | "自家人…" |
| 异象（天变/民谣） | 谶语、半白 | 飘忽 | 不给结论 | "夜观乾象…" |

### 3.3 AI 注入规范（见第六节）

每月把"常驻班底当前关系"注入 `PROMPTS.user`，AI 即可在事件/选项中**指名道姓**地引用，连贯性立现（"额亦都进曰…""舒尔哈齐阴有异志…"）。

---

## 四、旗标驱动的故事节拍阶梯（让抉择首尾呼应）

现有 `CANONICAL_FLAGS`（L2200）是 15 个语义词。当前它们只被结局读取（且多半靠 `derived` 反推）。我们**把它们变成"剧情里程碑"**：早期选择置位 → 中期事件门控该旗标出现 → 后期抉择再分叉 → 结局读取。这就是"散"变"连"的骨架。

### 4.1 六条结局的节拍阶梯（示例）

| 结局 | 早期置位（Act I–II） | 中期回响（Act III 门控） | 后期分叉（Act IV） | 终局读取 |
|------|----------------------|--------------------------|--------------------|----------|
| 一统寰宇 `unify` | 称汗/誓师类选项 `setFlags:["unify"]` | `gating.requiresFlags:["unify"]` 出"经略辽东" | `minStats.military≥55` 出"誓师入关" | `unify`+`military≥55` |
| 鼎革之主 `reform` | 变法/新政 `setFlags:["reform"]` | `requiresFlags:["reform"]` 出"清议反弹" `setFlags:["scholar_oppose"]` | `maxRelation.士大夫:-10` 出"台谏攻讦" | `reform`+`士大夫≤-10` |
| 治世仁主 `long_peace` | 惠民/宽政 `setFlags:["amnesty","tax_cut"]`→ 聚合为 `long_peace` | `requiresFlags:["long_peace"]` 出"治世祥瑞" | 维持 `people/court≥60` | `long_peace`+双≥60 |
| 权臣篡立 `usurped` | 猜忌边将 `setFlags:["scholar_purge"]`（误伤） | `maxRelation.边将:-20` 出"兵变" | `minStats.military≤25` | `usurped` |
| 清议倾国 `scholar_oppose` | 触怒士大夫 `setFlags:["scholar_oppose"]` | `requiresFlags:["scholar_oppose"]` 出"党争" | `court≥30` 出"朝局离析" | `scholar_oppose`+`court≥30` |
| 季世之主 `weak` | 横征/怠政 `setFlags:["tax_heavy"]` | `requiresFlags:["weak"]` 出"民变" `setFlags:["rebellion"]` | `treasury/people≤20` | `weak` |

> 注意：`setFlags` 同时由**预设事件 branches** 与 **AI 动态事件 outcomes**（L3044，已限 `CANONICAL_FLAGS`）写入，两者共用同一词表——这正是统一的关键：**作者编排的里程碑与 AI 即兴的语义，落到同一个旗标上**。

### 4.2 branches 字段写法（引擎就绪，挂接现有 schema）

> **⚠️ 字段口径（务必遵守）**：`branches[i]` 只承载 `setFlags` / `clearFlags` / `nextEventId`；**阵营好感（relations）由同一下标的 `relationDeltas[i]` 驱动**，写在 `branches` 里的 `relationDelta` 不会被 `applyChoiceEffects` 读取（属无效字段）。下方 mini 示例已清理为纯旗标；完整可落库且 schema 正确的版本见 `docs/努尔哈赤剧情脊柱示例.json`。

预设事件新增可选 `tier`/`gating`/`branches`，与 `choices` 一一对应，代码已支持（`pickGatedMajor` L2599、`applyChoiceEffects` L3010）：

```jsonc
{
  "year": 1618, "tier":"major",
  "gating": { "requiresFlags":["unify"] },   // 仅当已称汗建国才出现
  "title":"经略辽东",
  "choices":["迁都沈阳，虎视中原","抚纳汉民，行屯田安辑","征伐并行，以战养战","联蒙古，断明之臂" ],
  "branches":[
    { "setFlags":["unify"] },                                   // 深化统一线
    { "setFlags":["long_peace"] },                              // 关系变动见下方 relationDeltas
    { "setFlags":["unify","war_win"], "clearFlags":["weak"] },
    { "setFlags":["unify"] }
  ]
}
```

---

## 五、AI 提示词叙事注入规范（最高 ROI 的实现）

这是把"散"变"连"的**核心开关**：在 `PROMPTS.system` / `PROMPTS.user` 各加一段**叙事上下文**。无需改任何事件数据，仅让 AI 每月"知道自己在哪条线上"。

### 5.1 `PROMPTS.system` 增量（每帝一次）

```
【本朝主线索】
中央戏剧问题：{emperor.reignPremise}
四幕弧：定鼎(0-25%)→守成拓业(25-55%)→中局大考(55-80%)→晚岁遗烈(80-100%)
常驻班底：{cast 每名 → 角色·声线·初始关系}
史官口吻（贯穿事件描述与"史官评"，软约束）：{HISTORIAN_VOICE + voiceTag 变体，记—指—悬三段式}
```

### 5.2 `PROMPTS.user` 增量（每月）

在现有"【当前时位】…【当前朝局关系】"之后追加：

```
【叙事上下文·本月】
当前幕：{actName}（进度 {pct}%）
主线索进度：{依据 state.flags 渲染一句，如"已称汗立国，正图经略辽东"}
活跃旗标（视为未了之事）：{flags→悬语，如"reform:变法之议方兴，清议之波将起"}
常驻班底近况：{具名关系，如"额亦都(+42,忠勇)｜舒尔哈齐(+18,渐生嫌隙)"}
事件须：① 围绕主线索矛盾展开；② 可指名引用上述班底；③ 选项后果须呼应活跃旗标所代表的未了之事。
```

> **为什么这招最有效**：它让 AI 每月都"带着上一月的记忆"编事件——不是靠改写 1866 则数据，而是靠**每月喂同一根线**。散感的根因（0-1/0-2）被正面解决。

### 5.3 旗标→悬语映射表（注入用，可扩展）

```js
const FLAG_OMEN = {
  unify:"一统之业方启，四境多事之端已萌",
  reform:"更化之弓已满，清议之弦将断",
  long_peace:"治世之象初成，逸豫之渐亦伏",
  scholar_oppose:"士大夫之望已失，朝局离析之朕也",
  usurped:"爪牙生心于下，威柄渐移之危也",
  weak:"府库民命俱敝，社稷飘摇之候也",
  war_win:"武功既著，穷兵之戒不可忘",
  war_loss:"师丧气挫，雪耻之机当图",
  tax_heavy:"横征之怨在野，民变之烽将燃",
  rebellion:"叛火已燃，剿抚两难"
};
```

---

## 六、落地样例 · 清太祖努尔哈赤 统一剧情

下面给一条**完整可落库**的剧情脊柱，证明框架在真实数据上跑得通。

### 6.1 主线索 + 中央戏剧问题

```
reignPremise: "边徼部族之主，能否以武力立国、突破中原千年之防？
凭十三遗甲起兵，诸部初附而离心未消，汉民未附而明边已惊。
穷兵则基业不稳，怀柔则霸业难成——此其一生之张力。"
voiceTag: "质朴"
```

### 6.2 四幕节拍（对应现有事件）

| 幕 | 触发的既有/新增事件 | 旗标动作 |
|----|---------------------|----------|
| I 定鼎 | 赫图阿拉称汗（1616） | 选"称汗建国" → `unify` |
| II 守成·拓业 | 七大恨誓师（1618）、萨尔浒（1619） | `unify`+`war_win` |
| III 中局大考 | **新增** 经略辽东（gated `unify`）、迁都沈阳之议 | `long_peace`/`unify` 深化；若猜忌弟 → `scholar_purge` 误伤 |
| IV 晚岁·遗烈 | 立储之争（继承危机，midCrisisAnchors）、宁远之挫 | `unify` 收束 → 结局 一统寰宇 |

### 6.3 常驻班底

```jsonc
"cast":[
  {"id":"eyidu","name":"额亦都","role":"宿将","faction":"边将","init":42,"voiceTag":"悍直","arc":"微时伴驾，晚岁骄倨"},
  {"id":"shuerhaci","name":"舒尔哈齐","role":"宗亲·弟","faction":"宗室","init":55,"voiceTag":"郁勃","arc":"功高见忌，终至失和"},
  {"id":"gongzhenglu","name":"龚正陆","role":"谋主·汉儒","faction":"士大夫","init":18,"voiceTag":"谨婉","arc":"倡抚汉民，不见信于贵胄"}
]
```

### 6.4 可直接落库的事件 JSON（含 branches/gating，挂接现有 schema）

> 下面 3 则为**新增/补强**事件，字段与 `08-qing.json` 现 schema 完全兼容（`year/month/title/description/choices/historicalChoice/relationDeltas` 原有；`tier/gating/branches` 新增可选）。可直接并入 `qing_taizu.events`。

```json
{
  "year": 1618, "month": null, "tier": "major",
  "gating": { "requiresFlags": ["unify"] },
  "title": "经略辽东",
  "description": "萨尔浒大捷后，后金势振，然新附诸部离心、辽东汉民未附。或言当迁都沈阳以临中原，或言宜先安辑境内。舒尔哈齐密谓左右：『兄志在天下，吾辈可得喘息乎？』",
  "historicalChoice": 0,
  "choices": [
    "迁都沈阳，虎视中原",
    "抚纳汉民，行屯田安辑",
    "征伐并行，以战养战",
    "联蒙古诸部，断明之臂"
  ],
  "historicalOutcome": "后金据辽东而都沈阳，明边自此大震",
  "id": "qing_taizu_e_liangdong",
  "relationDeltas": [ {"边将":3}, {"士大夫":5,"商贾":3}, {"边将":2,"士大夫":-2}, {"边将":4} ],
  "branches": [
    { "setFlags": ["unify"] },
    { "setFlags": ["long_peace"] },
    { "setFlags": ["unify","war_win"], "clearFlags": ["weak"] },
    { "setFlags": ["unify"] }
  ]
}
```

```json
{
  "year": 1621, "month": null, "tier": "major",
  "gating": { "requiresFlags": ["unify"], "minRelation": {"宗室": -15} },
  "title": "兄弟之隙",
  "description": "舒尔哈齐以功高见忌，阴结部曲，欲别居自立。额亦都请诛之以定人心，龚正陆谓当婉全骨肉。帝徘徊两者之间。",
  "historicalChoice": 1,
  "choices": [
    "幽锢其弟，收其部曲",
    "赐第优容，阳尊而阴制",
    "分土封之，以示不疑",
    "付额亦都便宜行事"
  ],
  "historicalOutcome": "舒尔哈齐忧死，诸子离心，后祸犹伏",
  "id": "qing_taizu_e_xiongdi",
  "relationDeltas": [ {"宗室":-10,"边将":3}, {"宗室":2}, {"宗室":5,"边将":-3}, {"边将":4,"宗室":-5} ],
  "branches": [
    { "setFlags": ["scholar_purge"], "clearFlags": ["long_peace"] },
    {},
    { "setFlags": ["long_peace"] },
    { "setFlags": ["scholar_purge"] }
  ]
}
```

```json
{
  "year": 1626, "month": null, "tier": "major",
  "gating": { "requiresFlags": ["unify"], "minStats": {"military": 55} },
  "title": "宁远之挫与遗命",
  "description": "帝亲征宁远，遇袁崇焕坚城红夷大炮，师老无功，疽发于背。弥留之际，立储未决，诸子与各旗主各怀心思。",
  "historicalChoice": 0,
  "choices": [
    "命八旗共议，立贤不立长",
    "遗命长子继位，咐诸贝勒辅政",
    "分八旗为八分，各固本支",
    "托孤于额亦都，总摄军政"
  ],
  "historicalOutcome": "八王共治之制初立，大清基业乃固",
  "id": "qing_taizu_e_yiming",
  "relationDeltas": [ {"宗室":3,"边将":2}, {"宗室":5}, {"宗室":-4}, {"边将":5} ],
  "branches": [
    { "setFlags": ["unify"] },
    { "setFlags": ["unify","long_peace"] },
    { "setFlags": ["unify"] },
    { "setFlags": ["unify"] }
  ]
}
```

### 6.5 史官评示例（弧线指针）

- Act II 萨尔浒后：`"帝集中兵力，破明四路之师。论者谓：此武功之极，亦穷兵之始。"`
- Act III 兄弟之隙（选幽锢）：`"帝幽其弟而收其众，宗亲之衅自此始。惜乎！打天下易，守一家难。"`
- Act IV 遗命：`"八王共治之制立，基业粗定。然储贰未一，后日之祸伏于此时矣。"`

---

## 七、叙事-玩法整合矩阵（现有机制 → 叙事节拍）

| 现有机制（代码位置） | 叙事身份 | 玩家感受到的"统一" |
|----------------------|----------|--------------------|
| 六维 `stats`（L2067） | 国力脉象 | 数值起伏 = 剧情身体 |
| `computePressure` 高斯峰（L2567） | 第三幕"大考"的戏剧张力 | 中段自然更紧张 = 剧情高潮 |
| `midCrisisAnchors`（L2608） | 每位皇帝的"标志性中年危机" | 每人都有记忆点事件 |
| `CANONICAL_FLAGS` + `gateOK`（L2587） | 抉择的蝴蝶效应链 | 早年的选择，中晚年回响 |
| `state.relations`（L2067） | 人物关系网 | "我认识这些人" |
| `ENDINGS_RULES`（L3231） | 主线索的六种归宿 | 不同玩法 → 不同盖棺 |
| `FAIL_DIMS` 崩盘（L2206） | 国力维度"达摩克利斯之剑" | 任意一根线断 = 亡国叙事 |
| `historicalAnchors`（L2575） | 真实感的"空气" | 日常月也闻得到史实 |
| 史官点评（L3070→改） | 弧线指针 | 每月都被提醒"你在哪条线上" |
| 科技树 `tech`（L2287） | "穿越者之能"的代价线 | 超前科技 → 保守派阻力事件 |

---

## 八、环境/时代叙事（Era Storytelling）

统一感不只来自事件，也来自**空间与时代氛围**：

- **`eraContext` 升级为"开场定调"**：当前只在 system 提示出现。建议在皇帝"intro"屏加入一句"时代之叹"（由 `reignPremise` 派生），让玩家即位前就听见主线索。
- **每幕氛围词**（注入 AI）：Act I「草创」/ Act II「鼎盛」/ Act III「多事」/ Act IV「晚照」——AI 据此调事件色调。
- **终局收束即"史论"**：`end-review`（L3288）已是盖棺定论，只需把主线索的"张力是否兑现"写进 `review`（现行 6 条结局 review 已具雏形，可按 `reignPremise` 微调语气）。

---

## 九、实施批次与兼容性

### 9.1 数据字段（全部可选，零回归）

| 字段 | 位置 | 用途 | 缺失时 |
|------|------|------|--------|
| `reignPremise` | `emperor` | 主线索/中央戏剧问题 | 走原逻辑（无注入） |
| `voiceTag` | `emperor` | 史官声线微调 | 默认通用史官声 |
| `cast[]` | `emperor` | 常驻具名班底 | 仅用 4 阵营桶 |
| `tier`/`gating`/`branches` | `emperor.events[]` | 旗标门控与节拍链 | 按年命中原逻辑 |
| `midCrisisAnchors` | `emperor` | 第三幕定时炸弹 | 无强制危机（已有兜底） |

### 9.2 提示词改动（纯增量）

- `PROMPTS.system`：加 5.1 段（每帝一次）。
- `PROMPTS.user`：在现有"【当前朝局关系】"后加 5.2 段。
- `flagOmen` 映射表 + `actName(progress)` 小函数（新增，不入主流程）。
- `stanceCommentText` 改造为读 `flags`+幕（可选，P1）。

### 9.3 验收 KPI（对应"散→连"）

| 指标 | 现状 | 目标 | 验证法 |
|------|------|------|--------|
| 月度事件"回指主线索"率 | ≈0% | ≥60% AI 月含主线索关键词 | 抽样 50 月 AI 输出 grep `reignPremise` 关键词 |
| 具名人物月引用率 | 0% | ≥40% AI 月指名班底 | 同上 grep cast 名 |
| 同帝可达结局数 | 多靠数值兜底 | ≥3 条靠旗标链可达 | 自测"同帝不同线" |
| 旗标链回响（早期→≥10月后门控） | 0 例 | ≥1 例/代表帝 | 跑努尔哈赤样例 |
| 史官评"弧线指针"占比 | 0% | ≥50% 点评含悬语 | 抽样 |

### 9.4 批次建议（与既有优化路线图对齐）

| 批次 | 内容 | 成本 | 说明 |
|------|------|------|------|
| **P0（最值）** | `PROMPTS` 注入主线索+幕+班底（第六节） | 低（纯提示词） | 立竿见影解决"散" |
| **P1** | 史官声线 Voice Pillars + `flagOmen` 悬语 | 低 | 提升统一"质感" |
| **P2** | 为 5–10 位代表帝补 `reignPremise`/`cast`/`branches` 节拍链 | 中（数据工程） | 让旗标链真实落地 |
| **P3** | 全套 262 帝 `reignPremise` + intro 时代之叹 | 高（可分批/借 AI 抽取） | 覆盖全部 |

---

## 十、一句话收尾

"散"不是因为事件少，而是因为**每月事件没被同一根线串起来**。现有代码已经备齐了串线的所有钩子（`flags`/`relations`/`pressure`/`midCrisis`/ endings），本框架只做一件事：**让叙事层把这些钩子用起来**——用史官的统一声线做"线"，用每帝主线索做"针"，用常驻班底和旗标节拍做"穿引"。最高 ROI 的一步，是第六节那两段提示词注入：不动一行事件数据，却能让 AI 每月"记得自己在哪里"。
