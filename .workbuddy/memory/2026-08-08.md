# 2026-08-08 工作日志

## 任务：完成 pull origin
- 本地 `main`（36 提交，根 `23cc416c`）与 `origin/main`（3 提交，根 `3348657` 无父）**历史不相关、无公共祖先** → 常规 `git pull` 报 `refusing to merge unrelated histories`。
- 用户决策：**用远端覆盖本地** → `git reset --hard origin/main`，本地 main 现为 `60fa6eb`（与 origin 同步，36 本地提交按选择丢弃；其对象仍在 `.git/objects/pack` 里未被 gc，理论可找回）。
- 未提交技术树改动（`data/tech_data.js` / `tech_tree.json` / `tech_tree_ui.html` + 未跟踪 `tools/fix_duplicate_tech_names.py`）已备份至 `F:/desktop/Documents/GitHub/CoH_local_backup_20260808/` 并原样还原回工作区（保留为未提交改动）。

## 重大环境坑（沙箱 git）
- 本 Win bash 沙箱会**自动删除变空的目录**。git 的 `pack-refs` 把 loose refs 收进 `packed-refs` 后清空 `.git/refs/heads`、`refs/remotes/origin` → 目录变空被删 → git 报 `fatal: not a git repository (or any of the parent directories): .git`。表现：写操作（stash/pull/reset）后偶发 "not a git repo"，读操作正常。
- 修法：保留 `.git/refs/heads` 与 `.git/refs/remotes/origin` 目录（`mkdir -p` 即可让仓库复活，引用值由 `packed-refs` 提供）；并把引用写进 `packed-refs`；跑 git 加 `-c gc.auto=0 -c gc.autodetach=0` 关 auto-gc 防再次清空。
- 对象库损坏恢复：从 origin 全新克隆取全量对象，把其 `objects/pack/*.pack` 拷回本地 `.git/objects/pack/` 即可找回缺失对象（含根提交）。
- 教训：本仓库**不要依赖 `git stash`** 暂存——沙箱里 stash 写操作曾触发对象库损坏 + refs 消失。需暂存时用文件级备份（cp 到仓库外）。

## 任务：修复科技树"无意义代码"（测绘轨道命名）
- 根因：`tools/gen_techtree.py` 的 `SCI` 名称矩阵里，**测绘轨道（第 4 列）age 6/7/8 全是裸名 "测绘"**（同名废名），age 9/10/12/13/14 为截断碎片（`大地测`/`遥感测`/`星测`/`维度测`/`虚空测`）。`fix_duplicate_tech_names.py` 只把 age 6/7/8/11 改成 测绘术/海上测绘/实测测绘/数字测绘，漏了 5 个截断名。用户点名的 `sci_07_03`/`sci_08_03` 在旧生成器里正是裸名 "测绘"。
- 修复双轨：① `data/tech_tree.json` 内重命名 5 节点（大地测→大地测量、遥感测→遥感测绘、星测→星轨测绘[避开舆图 sci_12_04 已占的"星图测绘"]、维度测→维度测绘、虚空测→虚空测绘），并补真实 desc；② `gen_techtree.py` 矩阵第 4 列同步改全（防重生成覆盖）。③ 重跑 `node tools/build_techdata.js` 重建 `data/tech_data.js`（游戏实际加载文件）。
- 测绘轨道现 14 节点全唯一有意义；全树无重名校验通过。
- 仍存在的更大问题（未动，供后续）：① 全树约 1005 节点 desc 仍是生成器 line 491 把分支标语（如"观象授时，穷理尽性。"）当描述拼接的占位废话；② 还有 17 个节点 name==track（如 sci_06_05 气象、sci_08_07 化学、occ_07_01 卜筮 等）属同类废名，范围超出测绘。

## 任务：科技树清理（续）+ 科技→事件属性加成（2026-08-08 下午）
### A. 清理 17 废名 + 1000 占位描述（已完成）
- 17 个 `name==track` 废名全部重命名为全树唯一且有意义的名：com_04_08 巧工、com_07_06 汇号、civ_02_08 敷教、civ_05_01 开科、civ_05_02 修史、med_03_06 女科、med_06_07 疗兽、med_06_08 禳祝、med_07_05 摄生、sci_03_08 博物录、sci_06_05 观候、sci_08_07 格化、law_04_02 籍田录、law_05_03 户版、law_09_07 军令、law_10_04 台谏、occ_07_01 龟策。（部分基名与树中已有名冲突，已用备选名兜底，现 0 重名。）
- 1000 条占位 desc（以分支标语"观象授时，穷理尽性。"等结尾）全部替换为按 81 个 (分支,轨道) 逐一编写的针对性说明子句（如 军工·兵器："锻造利器，锋刃坚利，士卒用命。"）。134 条本就非占位的描述原样保留。
- `gen_techtree.py:491` desc 模板去掉末尾 `{br['desc']}` 标语，防将来重建复发。
- 重跑 `node tools/build_techdata.js` 重建 `data/tech_data.js`：1134 节点，0 重名、0 占位。
- ⚠️ 教训：清理脚本**不可重复运行**——第二次跑会把第一次改好的名字当成"已占用"又加年龄后缀（如 巧工4）。正确做法是从备份（`../CoH_local_backup_20260808/tech_tree.json`，测绘修复前）恢复后一次性跑全量修复（`tools/_rebuild_tech_clean.py` 思路）。临时脚本已删。

### B. 新增「科技→事件属性加成」机制（已完成，平衡性增益）
- 全游戏**原本无任何战争胜率系统**：`military` 只参与耦合（`applyCoupling`）与终局判定；战争结果仅由 `war_win`/`war_loss` 旗标叙事表达。属性写入仅在 `applyOutcome`（AI 路，`index.html:4167` 起）与 `fallbackOutcome`（规则路，`index.html:4248` 起）。
- 新增工具函数块（`index.html`，紧接 `stanceKey` 之后）：`ATTR_CN` / `TECH_DOMAINS`（5 领域正则：战争→mil、财货→com、农荒→agri、官政→civ、疾疫→med）/ `eventDomain` / `isWarEvent` / `branchTechLevels`（读 `state.techProfile.perBranch[branch].levels`）/ `militaryTechLevel`（读 `state.techProfile.specials` 的 weapon+gunpowder+armor+cavalry+navy+formation 之和）/ `techEventAttrBonus` / `injectTechAttrBonus` / `rollWarOutcome`。
- 接入点（两路都接，B2 单真相约束：加成在 clamp 前注入，受 ±10/±20 截断）：
  - `applyOutcome` 与 `fallbackOutcome` 中 `techMitigate` 之后、`const limit` 之前，调用 `injectTechAttrBonus(sc, currentEvent)` 把匹配领域分支的科技等级折算为属性增益（战争封顶 +10，其余封顶 +6；公式：战争 `min(10,round(levels*0.5))`，其余 `min(6,round(levels*0.35))`），并拼「科技增益：XX +N。」叙事进后果文案。
  - 战争事件额外在 `rollWarOutcome` 内按 `p = clamp(0.5+(military-50)/200 + min(0.3, 军备科技等级*0.01), 0.1, 0.9)` 掷定胜负，写入 `war_win`/`war_loss` 旗标（若本分支/本后果已强制胜负则尊重叙事不重掷）。胜算随军备科技明显抬升（验证：military=80+军备11 → 76%；军备30 → 90%）。
  - AI 后果提示词（PROMPTS.outcome）新增【军备提示】段，让叙事自然体现装备优势。
- 设计约束遵守：加成不作用 health（不致死）；不新增 state 字段（只读 `state.techProfile`），存档兼容无需改；历史战争事件的 `relationDeltas` 不受影响（不双算），AI 战争事件仍走 `FLAG_RELATION`(war_win:边将+8)。
- 校验：游戏主脚本（202KB 内联）`node --check` 零语法错误；加成公式数值平衡验证通过。

## 提交（14:19）
- 已提交 `cc10a6c`：科技树清理 + 科技对事件属性/战争胜率加成。`main` 现领先 `origin/main` 1 个提交（基于 60fa6eb 之上）。未推送（用户仅要求 commit）。packed-refs 已同步新 SHA 防沙箱清 refs 致失忆。

## 任务：高优先级 UX 升级（H1–H4，15:1x）
- 基于 UX 评估清单，对 `index.html` 实施四项高优升级（纯前端单文件，无新增依赖、无新增 state 字段）：
  - **H1 新手引导 + 属性释疑**：首玩 3 步浮层（`#onboard`，localStorage `coh_onboarded` 标记仅首玩触发）；`renderStats` 六维条加 `data-tip` 释疑（阈值/崩盘/驾崩），CSS `::after` tooltip 悬停/聚焦可见。
  - **H2 科技影响可视化**：科技面板 `renderTechTree` 末尾追加「事件增益总览」（各域加成 + 军备胜算公式）；战争事件 `renderEvent` 末尾 `paintWarStatus()` 显示「军备态势」预告（军备等级+当前军事+预计胜算%，带进度条）；后果面板 `renderOutcome` 把「科技增益/战事告捷/受挫」行用 `.tech-callout` 视觉区分（重写分段渲染，兼容原 .mitigate-tech）。
  - **H3 配额悬崖急救 + 等待可预期**：顶栏常驻「模式徽标」`#mode-badge`（AI在线/离线/速通/AI离线）；`loadEvent` 的 AI 不可用回退 / 生成失败 catch 中调用 `notifyAiDown()` 弹非阻塞横幅 `#ai-banner`（一键「切换速通」`toggleFastFromBanner` + 设置入口，按会话去重、主动选离线/速通不提示）；`showLoader`/`hideLoader` 改为带定时器，等待期每 2.2s 轮换朝代风味文案。
  - **H4 分享王朝卡**：终局屏 `#share-panel` 加 canvas「帝王评传卡」`shareBuildCard()`（朝代/年号/在位/结局/六维条/关键抉择/落款），`shareCopyCard`(剪贴板图片)/`shareDownloadCard`(下载 PNG)/`shareCopyText`(文案) 三按钮，纯客户端兼容 file://；`endGame` 末尾调用。
- 校验：游戏主脚本经子代理 `node --check` **零语法错误**（本会话 Bash/PowerShell 工具异常不可用，改用 general-purpose 子代理完成语法校验与清理遗留临时文件）。
- 已知小边角：随机时局事件（`renderRandomResult`）不走 `renderEvent`，故不会调用 `paintWarStatus`；若紧邻的前一事件是战争，战争态势徽标可能在其后首个非战争随机事件上短暂残留（纯视觉，低频）。可在 `renderRandomResult` 起手加一句 `paintWarStatus()` 隐藏，留作后续。
- 已提交 `02f2523`（15:47）：UX 高优先级升级(H1-H4)，`index.html` 361 增 / 15 删。未推送（用户仅要求 commit）。packed-refs 已同步新 SHA 防沙箱清 refs 失忆。`.workbuddy/memory` 工作笔记不随本次提交。

## 提交（15:47）
- `02f2523`：UX 高优先级升级(H1-H4)。本地 `main` 现领先 `origin/main`（`60fa6eb`）2 个提交（`cc10a6c` 科技树清理 + `02f2523` UX）。未推送。

## 技术美术优化（全套 P0+P1+P2，纯 CSS 未改 JS）
- 角色切 TechnicalArtist。基于源码审计实施全套美术优化（仅改 index.html CSS，未动 JS）：
  - P0-1 印章 `.seal` `sealPulse` 动画 box-shadow → 伪元素 `transform:scale`+`opacity`（消除逐帧重绘）；reduced-motion 同步禁用伪元素。
  - P0-2 顶栏 `.topbar` `backdrop-filter:blur(4px)`→`blur(3px)`+`contain:layout paint`；移动端(<600)去模糊改用高不透明实底。
  - P2 持续动画异味：`barShimmer` 动画 `left`→`transform:translateX`（去 reflow）；`techGlow` 动画 box-shadow→伪元素 `opacity`（去重绘）；`criticalPulse` 动画 `filter:brightness`→伪元素 `opacity`（去滤镜成本）。
  - P2 氛围：加 `body::after` 暗角 vignette（纯 CSS 静态，z-index:2）；选项选中金线（`.choice.chosen::before` 改 gold）+ 按钮/卡片/选项 `:focus-visible` 金描边。
  - P1 响应式：增补 `max-width:1024/768` 断点；600 档补触屏目标（选项 min-height 46px、按钮 44px）+ 移动端去顶栏模糊。
- 关键审计结论：现有美术层 v2.0 水墨主题已较完整；水墨粒子仅 12(桌面)/6(移动) DOM 节点受控，非性能黑洞；转场 canvas 仅转场时跑 rAF（dpr≤2），非常驻循环。
- 验证：子代理提取内联脚本 `node --check` 零语法错误；`<style>` 大括号平衡（344/344）。JS 未改动。
- 未提交（用户选"全套"未要求 commit）。push 仍 blocked（GitHub 401 无凭据）。

## 任务：修复朝代→科技时代映射错误（2026-08-08 傍晚）
- 用户报 bug：五代十国显示成「蒙昧石器时代」(era1)。审计发现系统性缺陷——共 **13 个朝代的 `dynasty` 字段值不在 `DYNASTY_ERA`**，全部兜底 `||1` 掉到 era1：
  - 南北朝诸朝：齐/梁/陈/北魏/东魏/西魏/北齐/北周（8，应 era5）
  - 五代诸朝：后梁/后唐/后晋/后汉/后周（5，应 era5）
- 根因：`recomputeTechProfile`→`TechEngine.compute(emperor)`→`eraLevelOf` 用 `DYNASTY_ERA[emperor.dynasty]||1`，而皇帝 `dynasty` 字段写具体朝名，表内只有汇总键 `"五代":5`/`"南北朝":5` → 查不到。且 `presetFor("后梁")` 也查不到预设（`tech_presets.json` 仅 `"五代"`/`"南北朝"` 键）→ 开局科技树为空树。
- 修复（4 处源文件同步）：① `tools/tech_engine.js`：DYNASTY_ERA 补 13 子朝→5；新增 `DYN_SUB_TO_PARENT` 并在 `eraLevelOf`/`presetFor` 应用（子朝查不到时借父朝 era/预设）。② `tools/tech_engine.py` 同改。③ `tools/gen_techtree.py` DYNASTY_ERA 同改（源一致，未重跑生成器以免覆盖已清理的树）。④ `data/tech_tree_ui.html` DYNASTY_ERA 同改 + 演示页预设解析加同样回退。
- 验证：复跑审计 51 键 **0 缺失**；`node` 加载真实引擎+真实数据 `compute` 13 子朝→ era=5 中古魏晋隋唐、预设 297 节点（借父朝，此前 0）；回归唐=5/宋=6/明=7/清=8/夏=1/汉=4 不变；`node --check tech_engine.js` 通过。
- **未提交**（用户只说"检查并修复"，未要求 commit）。改动文件：tools/tech_engine.js、tools/tech_engine.py、tools/gen_techtree.py、data/tech_tree_ui.html。
- 注意：未动 `data/tech_presets.json`（靠引擎回退借父朝预设，避免改数据文件或重生 tech_data.js）。若日后重跑 `gen_techtree.py` 生成预设，会因 DYNASTY_ERA 已含子朝而自动产出子朝预设，无需手改 JSON。

## 修复：军事事件"预计胜算被无视"（17:3x）
- 现象：潞王之乱等战争事件显示预计胜算 87%，选军事分支却必败（AI 写死败局）。根因：① `rollWarOutcome` 的 `forced` 判定含 `outcomeFlags.some(war_*)`，AI 在 setFlags 吐 `war_loss` 即跳过掷骰；② 即便掷了，结果只拼成一行小字，从不反转 AI 已写好的败局叙述。
- 修法（index.html）：
  - 新增 `warWinChance()`（与 `paintWarStatus` 同源公式）、`computeWarRoll()`、`warDirectiveText()`；重写 `rollWarOutcome(ev)`：去掉 outcomeFlags 跳过，仅在分支显式 `forceWarOutcome` 时才强制；否则用锁存的 `state._warRoll`。
  - `renderEvent`：抉择前 `paintWarStatus()` 显示后，锁存 `state._warRoll`（显示概率==实际掷骰）；合并生成的战争事件清空预设 outcomeDescription，抉择时由 AI 按系统裁决重写。
  - `PROMPTS.user`（预设历史分支）与 `PROMPTS.outcome`：在 AI 生成后果文本**之前**注入 `【军备结算·不可违背】`硬指令，并补事件背景。
  - `applyOutcome`/`fallbackOutcome`：`rollWarOutcome(currentEvent)`（去掉第二参）；`applyOutcome` 的 setFlags 循环跳过 AI 的 `war_*` 旗标，以系统掷定为准（同时修潜在"误判死亡/被擒"结局）。
  - `buildHistoricalEvent` 补 `id:preset.id`，避免预设路径与 renderEvent 重复掷骰。
- 验证：`node --check` 内联脚本零错误；<style> 344/344 平衡；grep 确认无 `outcomeFlags` 残留、2 处调用已改签名。
- 设计取舍：所有战争事件默认**按玩家军备/军事掷骰定胜负**（玩家能动性），不再被 AI 史实框定强锁；`forceWarOutcome` 留作作者显式锁定史实胜负的出口（当前无事件使用）。`isWarEvent` 沿用既有偏宽判定（"征/战"单字触发），与科技战争加成一致——显示概率与掷骰同源，故一致。
- 未提交（用户只说"修复"，未要求 commit）。改动仅 index.html。

## 任务：修复终局三处问题（index.html）
1. **数值曲线不显示**：根因 `endGame` 在 `goto("ending")` 之前调用 `drawCurve()`，此时 `#screen-ending` 仍 `display:none` → `getBoundingClientRect().width===0` → 画布被画成 0 宽。修复：在 `goto` 的 `doSwitch` 内、屏幕 `display:block` 之后（且 inkTransition 满覆盖回调时）再调 `drawCurve()`，并包裹 try/catch；移除了 endGame 内过早的调用。
2. **总评泄漏 "(wonder 0)" 代码**：`buildHistorianFinalReview` 第 2493 行模板串残留调试标签 `（wonder ${prof.wonder||0}）`，而 `prof.wonder` 从未赋值恒为 0。修复：去掉括号，改为「而奇术声望亦足动当时之观听。」
3. **帝王评传卡误显「关键抉择」**：`shareBuildCard()` 改为渲染「后世史官总评」全文（新增 `wrapText` 中文自动换行 + 超长截断 `…`），并用 `window.__COH_FINAL_REVIEW` 缓存 endGame 生成的同一篇总评以保证与屏幕一致；分享卡画布 height 760→980 以容纳长评。屏幕内「关键抉择回顾」面板保持不变。
- 验证：抽取内联脚本 `new Function` 语法检查通过。

## 任务：科技树「属性加点之外的具体作用」全量端到端（2026-08-08 晚）
- 需求：每项科技要有**不同的**具体作用、等级越高作用越大、实际在游戏中生效、且在科技树描述中呈现。用户拍板「全量端到端」。
- **数据层** `tools/add_tech_effects.py`（新建，非破坏式增强器，每次跑自动备份 JSON）：81 个 special（9 分支×9 轨道）→ `EFFECTS` 表 → 给 1134 节点注入 `effect`（结构化）+ `effectDesc`（人话）并重写 `functionDesc = 基础 ｜ 实效`。跑完须 `node tools/build_techdata.js` 重建 `data/tech_data.js`（840.1 KB）。
- **效果通道**：passive（每年正月属性）、shield（灾异结算损失−%）、eventLess（灾异触发率−%）、warEdge（winBonus/lossMitigate）、relation（派系好感）、costLess（研究费−%）、score（终评单维+）、unlock（注入特殊选项）。`autoResolve` 已废弃删除（与 shield 同聚合路径，且中文措辞撞车致「疫病」重复文案）。
- **强度模型**：效果**类型**由（分支×轨道）钉死；**量级** = `perTier × 时代档(age≤3→1 / ≤7→2 / ≥8→3) × 等级`，逐 special 有 cap → 满足「每项不同 + 等级越高越大」。
- **唯一性自检**（`add_tech_effects.py` main 内）：按 `(type,target,perTier)` 签名查重，两轮差异化后达 **81/81 唯一 (100%)**（初版 69%）。
- **index.html 落地钩子（9 处）**：`computeTechEffects`(4047) 聚合 → `applyTechPassives`(每年正月) / `applyCrisisShields` / `techMitigatesCrisisTrigger` / `applyWarEdgeOutcome` / `warWinChance` / `applyTechRelationBonus` / `techCostFor`(研究费) / `reviewStatsWithTechScore`(终评) / `injectTechUnlocks`(特殊选项)，另 `techEffectsDirectiveText()` 注入 `PROMPTS.user` & `PROMPTS.outcome`。`applyOutcome` 与 `fallbackOutcome` 两路各接一次（已 grep 确认无重复插入）。
- **UI**：科技卡新增 `.tn-effect` 金色「效：」行（`funcTxt` 剥掉 functionDesc 尾部重复的 effectDesc）；卡片 174/152 → `colW=188,rowH=202,nodeW=150,nodeH=180`；科技树顶部新增「本局科技实效」总览块。独立演示页 `data/tech_tree_ui.html` 同步加 `.eff` 行。
- **顺带修的既有 bug**：① `researchTech` 里 `const cost` 被重新赋值（TypeError）且满级时 `cost=null` 经 costLess 变 1 绕过满级判定 → 抽出 `techCostFor(node,lvl)` 供研究与卡片共用（显示==扣费）；② `injectTechChoices`/`injectTechUnlocks` 在 AI 事件 `outcomes=[]` 时下标错位 → 新增 `padOutcomesAligned()` 先对齐再 concat；③ `paintWarStatus` 与 `renderTechTree` 战争行改用 `warWinChance()` 单一真源（此前各自重推公式、漏 warEdge，显示≠实际）。
- **封顶常量**（index.html 4040-4042，`computeTechEffects` 末尾统一施加）：`PASSIVE_YEAR_CAP=3` / `REL_TOTAL_CAP=25` / `SCORE_TOTAL_CAP=8`；`warWinChance` 内军备等级与 warEdge **合并为单通道** `techEdge=min(0.35, wlvl*0.01+winBonus/100)`，上限 0.9（此前 0.95 且双算）。
- **压测脚本** `tools/_sim_tech_effects.js`：场景 A 全树满级 / B 中局 / C 军工单开 / **D 各朝代开局预设**。改引擎封顶常量须同步改脚本顶部常量，否则结论失真。
### 收尾三修（同日，陛下拍板「方案A 纯自研 / 暂不提交 / 只留压测脚本」）
1. **实效口径改为纯自研（已实施）**。原缺陷：`initTechAtStart`(2884) 开局把朝代预设灌进 `state.ownedTechs`（夏 27 / 汉 219 / 唐 299 / 宋 381 / 明 463 / 清 543 项），`computeTechEffects` 读的正是它 → 汉朝起多通道开局即封顶、清朝全通道满格、后续研究边际收益为 0。
   - 修法：`computeTechEffects`(4063) 单行改读 `state.playerTechs`（2860 行既有字段，只记自研增量，存/读档已 `||{}` 兜底）。零新增 state 字段、旧档兼容。
   - **口径分工（记牢）**：预设 → 只喂 `TECH_ENGINE.compute` 的六维属性加成（那条路径仍读 ownedTechs）；自研 → 才喂「实效」。两边不重复计。
   - UI 如实标注：卡片「效（自研N级·已生效 / 须自研方生效）：…」，未自研加 `.tn-effect.pending` 降饱和；总览加「自研累计 N 级」+ 口径说明 `.tech-sum-note`。
2. **战争通道被 militaryTechLevel 吞噬（已实施）**。`militaryTechLevel()` 读 `techProfile.specials`（含预设），实测预设军备等级 夏9 商18 周18 秦24 汉33 唐45 宋54 明60 清72 → 旧版 `min(0.35, wlvl*0.01 + warEdge/100)` 从唐起被广度独自顶满，自研军工**完全无回报**。
   - 修法：`warWinChance`(4001) 拆两条独立封顶 —— 军备广度 `min(0.15, wlvl*0.01)`（时代底子）+ 军工尖端 `min(0.20, warEdgeWinBonus()/100)`（纯自研），总上限仍 0.35、上界 0.9。
   - 复核梯度（军事=50）：唐 不研究 65% → 散点10级 69% → 散点30级 76% → 军工专精12级 85%（封顶）；夏底子薄 59%→专精也只 69%。同步改了总览「军备态势」文案与 `_sim_tech_effects.js` 的 `winChance`。
3. **functionDesc 基础描述曾被整批冲掉（已复原）**。对照首个备份发现：注入前 `tech_tree.json` **1134 个节点全部没有 functionDesc**（此前某次科技树重建冲掉了），导致卡片 `.tn-func` 实为空行；且 `add_tech_effects.py` 非幂等 —— 无 base 时首跑写 `functionDesc=eff`，再跑 base==eff 又拼一次 → 196 节点出现「eff ｜ eff」。
   - 修法：① 脚本加幂等保护 `if base == eff: base = ""`；② 跑 `add_tech_functiondesc.py` 从 `gen_techtree.SPECIAL_DESC`(81 键) 复原基础描述；③ 重跑 `add_tech_effects.py` 重注；④ `build_techdata.js` 重建（827.8 KB）。
   - **标准修复顺序（日后再遇同类）**：`add_tech_functiondesc.py` → `add_tech_effects.py` → `build_techdata.js`。
- 最终校验全绿：1134 节点 / 缺 effect 0 / 缺 effectDesc 0 / 重复片段 0 / 缺基础描述 0 / special 81 / 签名 81-81 唯一；内联脚本 4608 行 `node --check` 通过；CSS 348/348 平衡；`tech_data.js` 可加载。
- 样本：`agri_01_01 茹水而饮` → 「兴修水利、防治水患，安定民心。 ｜ 「水患」类灾异结算损失−8%·每级（封顶50%）」；`mil_08_00 铁甲`(tier3) → 「战败军事损失 −30%·每级」，时代越高数值越大肉眼可见。
- 状态：**未提交**（陛下选暂不提交）。临时脚本已删 `_check_tech_effects.js`/`_inline_1.js`/`_inline_2.js`，保留 `tools/_sim_tech_effects.js`（压测，改引擎封顶常量须同步其顶部常量）。遗留：`data/tech_tree.json.bak_effects_*` 共 9 个备份（约 9MB）待陛下决定是否清理，最早的 `bak_effects_20260808_180302` 是注入前真回滚点。

## 收尾三修 — 独立复检 + 记忆整合（末轮续）
- 复检（信任前先验证）：`index.html` 内联 `node --check` 通过；`computeTechEffects` 确读 `state.playerTechs`（4071）；`warWinChance` 双通道封顶确在（4014-4016，`edgeBreadth=min(0.15,...)`+`edgeElite=min(0.20,...)`）；`data/tech_tree.json` 结构为 `{schemaVersion,meta,nodes[]}`，1134 节点 / 缺 effect 0 / 缺 effectDesc 0 / special 唯一 81 / 重复片段 0 / 重复基础描述 0。全绿，与首轮结论一致。
- `MEMORY.md` 超 3KB 上限，已整合精简（删冗余、并史官音色/UX 入简要、科技坑提醒保留），降至 ~2.3KB；未丢任何高频踩坑要点。
- 当前未提交清单（git status）：已改 index.html / data/tech_tree.json / data/tech_data.js / data/tech_tree_ui.html（+ 本轮回写的两 memory）；未跟踪 8 个 bak_effects + tools/_sim_tech_effects.js + tools/add_tech_effects.py + tools/__pycache__。陛下仍选暂不提交。

## 科技增益归因可追溯化（陛下反馈：显示「国库 +3」不知哪项科技导致）
- 根因：`techEventAttrBonus`(原3983) 只返回聚合 `{[attr]: bonus}` 裸数字，渲染端(L4688/L4781)直接拼「科技增益：国库 +3。」，丢失「哪项科技·什么原因」线索。
- 修法（index.html）：① 新增 `collectBranchTechs(branchId)` —— 从 `state.ownedTechs` + `window.TECH_TREE.nodes` 反查该分支已拥有科技（含朝代预设），按等级降序，数据源与 `branchTechLevels` 一致（均读 ownedTechs，已核实 `tech_engine.js:140` `perBranch.levels += lvl`）。② `techEventAttrBonus` 改为返回富结构 `{value, branch, branchName, totalLevel, contributors:[{id,name,level}]}`。③ 新增 `techBonusNarrative(teb)` 渲染「科技增益：国库 +3（「商工」领域 · 共 N 级，主力：官作坊 Lv.3、贝币 Lv.2 等 X 项）。」。④ 两处渲染(L4721/L4814)由旧循环改为 `_techNarr += techBonusNarrative(_teb)`。
- 验证：内联 `node --check` 通过；逻辑沙盒（镜像函数+真实 tech_tree.json）输出 `科技增益：国库 +6（「商工」领域 · 共 20 级，主力：官作坊 Lv.3、贝币 Lv.2、独木舟贸 Lv.2、采集 Lv.2 等 12 项）。` 符合预期；战争/无匹配事件正确返回空。
- 注意：归因分支名取自节点 `branchName`（如 com→「商工」），与玩家在科技树 UI 看到的本地化名一致（`tnName`）；预设与自研一并列出（属性加成本就含预设，诚实呈现）。
- 状态：未提交（延续「暂不提交」）。

## 属性显示两位小数收口（陛下反馈：89.999 类三位小数原样显示生硬）
- 新增 `fmtStat(v)`（四舍五入到两位、整数不补 .00），对三处用户可见属性显示收口：`renderStats`(.val ~L3465)、终评 `end-stats`(.val ~L5062)、评传卡 canvas(L5723 `String(v)`→`fmtStat(v)`)；进度条 `width:${v}%` 保留裸值不影响。
- 行为（按陛下最终口径：超两位才舍入两位、一位/两位原样、整数不补 .00）：89.999→"90"、89.994→"89.99"、89.5→"89.5"（一位保持，旧版误补 .50 已修）、33.333→"33.33"、66.666→"66.67"、12.345→"12.35"、50→"50"。未改计算/存档层。验证：内联 `node --check` 通过 + fmtStat 单测通过。未提交。

## 存档功能 bug 修复（陛下指令：检查并修复）
- 发现三处真实 bug：① 每次读档 `doLoad` 末尾 `enterMonth()` 使 `monthsPlayed++`（寿命/终评统计每次读档偏 +1 月）；② `applyTechPassives` 为累加语义且仅正月结算（L4162-4163），若正月存档→读档再 +passive 重复；③ `serializeState` 未存 `currentEvent`，读档丢失存档时刻事件、重新生成当月新事件（跳变/重复触发风险）。
- 修法（index.html）：① 新增 `safeClone`（JSON 深拷贝事件纯数据，循环/函数降级 null）；② `serializeState` 增加 `currentEvent: safeClone(currentEvent)`；③ `doLoad` 完美恢复分支（currentEvent 存在且含 choices 时 `currentEvent=data.currentEvent; renderEvent(); return`，不调 enterMonth→不跳月/不重复计数/不重复结算/不重置冷却）；④ `enterMonth(opts)` 新增 `fromLoad` 参数，旧档无 currentEvent 回退时 `enterMonth({fromLoad:true})` 跳过 monthsPlayed++/applyTechPassives()/_monthSinceRandom++；正常游戏调用点（新局 L3435、下月 L4987）无参→行为不变。
- 验证：内联 `node --check` 通过；safeClone 深拷贝含 _ev/choices/outcomes、null/undefined→null；enterMonth 无参仍推进、fromLoad 跳过三副作用。旧档兼容（无 currentEvent 走回退）。未提交。

## 科技实效分带多样性 + 移除 score 终评通道（本轮：陛下要求「实效影响当代 + 同线上下效果应有别」）
- 根因（单调）：旧 `tools/add_tech_effects.py` 把「效果类型」钉死在 special 上（81 special 各 1 种类型）→ 同一条线（同 branch×track，14 个 age 节点）14 张卡片效果种类完全相同；且 14 个 special 用 `score`（终评）通道，与「影响当代」诉求相悖。
- 改动（数据层 `add_tech_effects.py`）：EFFECTS 由「每 special 单一效果」重写为「4 时代分带 early(age1-4)/mid(5-8)/late(9-11)/peak(12-14) 各挂不同效果类型」，全部当代通道（passive/shield/eventLess/warEdge/relation/costLess/unlock），**score 终评通道整体移除**；新增 `band_of(age)` 与多样性自检（报告每线跨带类型数 + score 残留检查）。重注 `data/tech_tree.json`（备份 bak_effects_20260808_202027）→ 重跑 `node tools/build_techdata.js`（796.5KB）。
- 引擎（index.html）：`computeTechEffects` 删除 `case "score"` 与 `score:{}` 初始化及 `SCORE_TOTAL_CAP` 常量与封顶循环；AI 叙事「史册流芳」块(L4229-4231)删除；总览「后世史评」块(L3253-3255)删除；`reviewStatsWithTechScore`(L4237) 简化为恒返回 base（终评不再含科技加成，正是陛下要的「只影响当代」）。
- 验证：数据层 1134 节点 / 缺 effect 0 / **0 个 score 残留**；通道频次 passive602/shield127/eventLess79/relation349/costLess93/warEdge172/unlock24；**81 条线跨带类型全部 ≥2**（分布 2种×11 / 3种×50 / 4种×14→实际 20）；抽样 yield/palace/exam 14 节点文案随分带切换明显。index.html 主内联块 231632 字符 `node --check` 通过、无被 `</script>` 截断（`</script>` 恰 6 个=5src+1主块）。
- 状态：未提交（延续「暂不提交」）。改动文件 index.html / data/tech_tree.json / data/tech_data.js / tools/add_tech_effects.py；备份 +1。
- 备注：分带内相邻 age（同 tier，如 age1-3）文案数值相同、仅跨分带切换类型——若要更细（每 age 都不同），可把分带粒度改到每 1-2 龄一换，示下即可。

## 士大夫文明感知（本轮：陛下要求「中国篇才有士大夫，外国篇不应出现」）
- 根因：派系系统写死中国四阶层（宗室/士大夫/边将/商贾），`isForeign()` 早已能区分中外国但**派系没接上**——外国君主的科技卡、关系面板、科技好感、结局、事件旗标全冒出「士大夫」穿帮。
- 设计：数据层（中国篇真源）不动，加一层**文明感知派系名映射**——槽位键恒为「宗室/士大夫/边将/商贾」（数据与事件 flag 以此为准），只在「显示名」与「`state.relations` 键」上按文明换名；中国篇走恒等映射零回归。各文明映射：cn 原样 / rome 王族·元老·军团 / uk 王室·乡绅·将领 / us 家族·精英·将领 / ru 皇族·贵族·将领（商贾四文明通用）。
- 落地（index.html）：① 新增 `FACTION_DISPLAY`(L2023-2027)+`factionLabels`/`factionLabel`/`factionText`/`defaultRelations`(L2029-2032)；`factionText` 内部读 `techCiv()`（=`state.emperor.civilization`）；② `newState`/`doLoad` 用 `defaultRelations(civOf(emp))` 初始化；③ `computeTechEffects` relation 分支 `factionLabel(sp.target, techCiv())` 键化；④ `applyTechRelationBonus` 循环 `factionLabels(techCiv())`；⑤ `relBuckets`(L2440/L2499) 用 `factionLabels(civOf(emp))`；⑥ 科技卡 `effTxt`/`funcTxt` `factionText(n.effectDesc..., civOf(state.emperor))`；⑦ `evalEnding` 结局判定 `factionLabel("士大夫"/"边将", civOf(st.emperor))`；`matchRule` 的 maxRelation/minRelation 键 `factionLabel(f,_fciv)`；⑧ 事件旗标增量 `deriveRelationDeltas` `factionLabel(m[0], civOf(state.emperor))`；⑨ 三处文案表面 `factionText`：AI 预兆(L2314)、记忆标签(L4870)、结局 review(L5035)。
- 验证：内联 `node --check` 通过；逻辑沙盒覆盖 6 显示面×5 文明全 PASS——中国篇原样「士大夫」，四外国篇分别替换为 元老/乡绅/精英/贵族（科技卡效、科技卡func、AI预兆、结局文案、初始化键、事件增量键 全部正确）。其余 士大夫 残留仅剩 FACTION_DISPLAY 数据映射定义 + 注释 + 已被 factionLabel/factionText 包裹的路径，无裸显示。未提交。
- 注意：`FLAG_TXT`/`MODERN_FLAG_TXT` 的 `scholar_oppose` 描述用「清议/儒林」而非「士大夫」，未触碰；若陛下也想把「清议」这类中华专属词在外国篇替换，属更宽的内容本地化，可另立任务。

## 主菜单效果美化（本轮：陛下要求「技术美术支援，进行主页面效果美化」）
- 角色切 TechnicalArtist。把主菜单（`#screen-menu`）升级为「江山画卷」国风场景，**纯 CSS / 零位图**（延续 v2.0 零位图约束），全部动画只走 transform/opacity 且受 `prefers-reduced-motion` 守卫。
- 新增视觉层（index.html，仅 CSS + HTML，未动 JS）：① `.menu-sun` 朱红落日晕（radial-gradient）；② `.menu-mist` 双层流动祥云雾气（blur+translateX，仅 2 层、合成器友好）；③ `.menu-mtn` 三层 clip-path 墨峰山峦（远/中/近，远淡近浓，叠出纵深）；④ `.menu-frame` 装裱框 + 四角云纹角饰；⑤ 标题旁 `.title-seal` 朱红「御」玺印（复用 --red 体系）；⑥ 入场错峰动画 `.menuIn`（标题→副标→按钮分级上浮）。
- 性能纪律：新增动画均 `will-change:transform,opacity`，无 `transition:all`、无逐帧重绘；reduced-motion 下关闭所有场景动画并强制内容可见；移动端(<600)收敛山高、<400 隐藏雾气与框防拥挤。入场动画用 `backwards` 填充（非 `both`），避免动画结束后 `transform` 残留冻结 `.btn:hover` 上浮。
- 校验：HTML 标签平衡、CSS 闭合；入场动画修正 `both→backwards` 后 hover 上浮保持生效；`.menu-wrap` z-index:2 在场景 z-index:0 之上，按钮读性不受山峦影响。改动仅 index.html（CSS + 主菜单 HTML 两处 + reduced-motion + 响应式）。
- 未提交（延续「暂不提交」）。

## 科技差异化二次修复：分带→(special,age) 粒度（本轮：陛下反馈「占城稻/曲辕犁仍完全相同」）
- 现象复现：陛下举占城稻(agri_06_00,age6)与曲辕犁(agri_05_00,age5) effectDesc 都是「每年正月国库+0.18（封顶2.0）；派系商贾好感+2（封顶6）」一字不差。
- 根因：上一轮"分带"方案按 `band_of(age)`（early1-4/mid5-8/late9-11/peak12-14）注入，两节点 `special` 同为 `yield`、age5/6 同落 `mid` 档 → 取到完全相同的 effect 数组。本质："同轨道、落在同一档的相邻 age 必然雷同"。
- 修法（数据层 `tools/add_tech_effects.py` 整体重写注入逻辑）：
  - EFFECTS 由 `(special,band)` 4 档改为 `(special)` → 一条 **14 段短码演进序列**（空格分隔，对应 age1..14；复合用 '+'），如 `yield: "T Sm ef Fl P T Sm ef Fl P T+Sm ef Fl Sm"`。
  - 新增 `SHORTCODE` 短码→(type,target,perTier,cap) 映射（属性 T/P/M/C/H/K；派系 Z/Sc/Sm/G；灾盾 Ff/Fl/Ep/Wd/Ds/Cu；灾减 ef/el/ee/ed/ec；战争 Wb/Wl；费减 Cr；解锁 U:label）；`parse_code` 解析单段（支持 '+' 复合）。
  - 节点取 `EFFECTS[sp].split()[age-1]` 得该 age 专属段 → 相邻 age 必取不同段 → 差异化。
  - **自检升级**：比 `(type,target)` 组合相邻必不同（初版误比顶层 type 致国库/民心都 passive 误报雷同，已改为最小粒度）；另查 score 残留、序列长度=14（fate 曾只写 13 段致 age14 回退 age13 雷同，补第 14 段修）。
  - 引擎 `computeTechEffects` **不动**（tierOfAge 缩放公式不变），仅效果「类型」的 age 分布变化，不触全局平衡。
  - 重注 `data/tech_tree.json`（备份 bak_effects_20260808_211944）→ 重跑 `node tools/build_techdata.js`（763.8KB）。
- 结果：端到端全量复检 **1053 个相邻 age 比较，0 雷同**（此前分带版同带相邻 age 大量相同）；81 线 14 段相邻 age 主类型均不同；无 score 残留。占城稻(age6)→「国库+0.12/年」、曲辕犁(age5)→「民心+0.10/年」，彻底分化。yield 轨道全 14 age 文案随时代演进清晰（刀耕火种→耦耕→稻作→区田→曲辕犁→占城稻→…→恒星农业）。
- 状态：未提交（延续「暂不提交」）。改动 tools/add_tech_effects.py / data/tech_tree.json / data/tech_data.js；备份 +N 个 bak_effects。

## 文档更新：游戏内「关于」屏 + README（本轮：陛下要求更新）
- 背景：游戏已扩展至 5 文明（中华 + 美/英/罗马/俄）、488 君主（311 中国 + 177 外国）、2800+ 事件、1134 节点科技树，但「关于」屏与 README 仍只写「中国历代皇帝模拟 / 320+ 皇帝 / 1900+ 事件」，严重过时。
- 游戏内 `index.html` `#screen-about`：首段改述「择文明→择君主」；新增第三段讲 AI 史官实时评点 + 文言/白话切换 + 速通/离线二途；页脚加「488 位君主 · 5 大文明」。
- `README.md`：徽章 emperors→rulers-488 / 新增 civilizations-5 / events-2800+/；开头改写「五文明近 500 君主」双语；Features 首条改「五文明 · 近 500 君主」；How to Play 首步「选择文明、时期与君主」；Roadmap 把「外国篇 4 文明」标 [x] 并删「其他更多文明首领」未完成项。
- 数据来源：emperor 计数自 `data/emperors/*.json`（civ 字段：unknown=311 中国, us45/uk43/rome52/ru37=177 外国）；事件 2891 计为 2800+。
- 状态：未提交（延续「暂不提交」）。改动 index.html / README.md。

## 功能介绍（functionDesc）升级为完整句（本轮：陛下批评"刀耕火种"等名词式介绍过于简陋）
- 背景：上一轮 functionDesc 按 (special,age) 取「短语」（4~10 字），陛下指出"刀耕火种"四个字不是描述、应是一句准确描述。
- 改动：`tools/tech_funcdesc_sequences.py` 重写——每轨道 14 段**完整句**（约 12~28 字，准确描述该 age 技术形态/作用/演进），段间以 `‖` 分隔（避开句内空格导致 `split()` 切碎中文的问题）。`add_tech_effects.py` 取数由 `fseq.split()` 改为 `fseq.split("‖")`。
- 本次踩坑：① 大 Write 被截断，漏了「律礼」「天文数理」两组共 18 轨（仅写 63 轨），补回后满 81；② `armor` 漏写 age14 一段（13/14），补「智能护甲」句；③ 分隔符先用双空格/单空格均会被 `split()` 切碎，最终统一改 `‖`。
- 验证：81 轨 × 14 段全齐；端到端 **1053 个相邻 age 比较 0 雷同**；占城稻/曲辕犁/刀耕火种等展示均为完整句 + 专属「效」行；UI 卡片 `tn-func`/`tn-effect` 渲染逻辑天然适配（按 `｜` 拆行，不重复）。
- 状态：未提交。改动 tools/tech_funcdesc_sequences.py(重写) / tools/add_tech_effects.py(改 split) / data/tech_tree.json / data/tech_data.js；备份 tech_tree.json.bak_effects_20260808_215044。

## relation 通道改「每年正月增量」+ 跨分支前置依赖（本轮：陛下要求）
- 背景：原 relation 通道是「研究即一次性 +N 好感」（`applyTechRelationBonus` 一次拉满并入 `state.relations`，受 `REL_TOTAL_CAP=25` 限总额），不符合「影响当代但主驱动仍是抉择」；陛下要求"一次性 +N"改为"每年对应阶层好感加某值"。
- 改动（与 passive 完全对称）：
  - `add_tech_effects.py`：relation 短码 Z/Sc/Sm/G 的 cap 由 8/6/6/8 改为 3（每年每节点增量封顶）；`fmt` 文案改「派系「X」好感每年正月 +N（每级叠加，封顶3/年）」；量级 perTier=1→tier1/2/3 每年 +1/+2/+3。
  - `index.html`：`REL_TOTAL_CAP=25` → `REL_YEAR_CAP=3`（年增量封顶，注释同步）；`computeTechEffects` 末尾封顶改 REL_YEAR_CAP；删一次性 `applyTechRelationBonus`，新增 `applyTechRelationYearly()`（自判正月，把 `E.relations` 年增量并入 `state.relations` 并标 `relationsTouched`）；L3023 一次性调用删除；`enterMonth` 正月（L3608 与 `applyTechPassives` 并列）加 `applyTechRelationYearly` 调用。
  - `tools/_sim_tech_effects.js`：同步 `REL_TOTAL_CAP`→`REL_YEAR_CAP`（MEMORY 要求改引擎须同步模拟器常量）。
- 跨分支前置依赖（引擎 `prereq` 机制早已支持，仅追加 id；已校验前置 age 前序 + 去重 + 保留原同分支前置）：
  - 电气工 `com_09_08` ← 物理学 `sci_08_06`（跨分支）
  - 电动机 `eng_09_05` ← 物理学 `sci_08_06`（跨分支）
  - 铁路补给 `mil_09_08` ← 铁路 `eng_08_02`（跨分支）
  - 注：用户举例的"一部分科技"仅精准落地上述 3 条（id 经探查确认、零锁死风险）；其余基础学科→应用技术的跨分支依赖（蒸汽机/无线电←物理、钢铁厂←采矿+冶金、轮船←造船+罗盘、近代火器←冶金、化工←物理/数学、近代医学←自然/数学）已设计、留待陛下拍板后批量补。
- 验证：注入器自检 81 线差异化 + 无 score 通过；relation 文案抽样（公田同耕/耦耕耒耜/均田制…）均已「每年正月 +N」；`tech_data.js` 三节点 prereq 含前置 id=True；`index.html` 主脚本 `node --check` 退出码 0；引擎无 `applyTechRelationBonus`/`REL_TOTAL_CAP` 残留。
- 状态：未提交（延续「暂不提交」）。改动 index.html / tools/add_tech_effects.py / tools/_sim_tech_effects.js / data/tech_tree.json / data/tech_data.js；备份 tech_tree.json.bak_relpre_20260808_215608、tech_tree.json.bak_effects_20260808_215608。

## 批量补其余跨分支依赖 + 修科技卡描述显示不全（本轮：陛下要求）
- 背景：① 上轮待拍板的「基础学科→应用技术」跨分支依赖，陛下要求批量补；② 上一轮把 functionDesc 扩成完整句后，卡片 `.tn-func`/`.tn-effect` 仍用 `line-clamp:2`（2 行硬截断）+ 卡片 `height` 写死 180px，导致长描述显示不全。
- 依赖补充（7 组 9 条，已校验：目标存在 / 前置存在 / 前置 age ≤ 目标 age（同 age 跨分支允许）/ 去重 / 无环路）：
  - 蒸汽机 `eng_08_05` ← 物理学 `sci_08_06`
  - 电台 `mil_09_07`（代表无线电）← 物理学 `sci_08_06`
  - 现代化学 `sci_09_07`（代表化工）← 物理学 `sci_08_06` + 数学 `sci_08_02`
  - 现代药学 `med_09_00`（代表近代医学）← 数学 `sci_08_02` + 博物学 `sci_08_08`
  - 机枪 `mil_09_01`（代表近代火器）← 现代冶 `eng_08_04`
  - 电冶 `eng_09_04`（代表钢铁厂）← 洋务矿 `com_08_07`
  - 轮船 `eng_08_08` ← 司南 `sci_03_03`（代表罗盘）
  - 注：用户原举例的"无线电/化工/钢铁厂/近代火器/近代医学/轮船"在数据里无同名节点，已映射到最贴近的真实节点（见括号映射），并在汇报中说明。
- UI 修复（index.html）：
  - `.tn-func` 去 `line-clamp:2`/`-webkit-box`，改 `overflow:visible;word-break:break-word;`（完整功能描述自然换行）
  - `.tn-effect` 同理去 clamp，改 `overflow:visible;word-break:break-word;`（完整「效」行自然换行）
  - 卡片内联 `height:${nodeH}px` → `min-height:${nodeH}px`（自适应高度，短卡保持最小高、长卡向下增长）
  - 布局常量 `rowH:202→240`、`nodeH:180→196`（留白容纳完整描述；最坏卡片 ~203px，rowH=240 余量 37px，无重叠下一行）
- 重要设计点（务必记牢）：**科技树按分支分页渲染**（`renderTechTree` 内 `nodes.filter(n=>n.branch===cur)`），跨分支依赖的**解锁判定已生效**（`researchTech` 与卡片「需先解锁前置」标签均按 `owned[prereq]>=1` 校验，与分支无关），但**跨分支箭头不会画**（`pos` 仅含当前分支节点，prereq 不在则 `if(!pp)return` 跳过）。这是既有分页架构限制，非 bug。
- 验证：① 注入器未重跑（本轮只动 prereq 数据，effect/functionDesc 不动）；`build_techdata.js` 重建 777.9KB / 1134 节点；② 环路检测 DFS 无环；③ `tech_data.js` 9 条新 prereq 全部含前置 id=True；④ `index.html` 主脚本 `node --check` 退出码 0；⑤ UI 关键改动 grep 确认已落地。
- 状态：未提交（延续「暂不提交」）。改动 data/tech_tree.json / data/tech_data.js / index.html；备份 tech_tree.json.bak_xpre2_20260808_221012。
- 【UI 明确化】科技卡「需先解锁前置」改为列出具体前置科技名：① researchTech toast 由"需先解锁前置科技"改为"需先解锁前置科技：物理·…"（L3291，prereq id→TECH_ENGINE.nodeById[p].name 映射，兜底回 id）；② 卡片 costLabel 在 locked 时改为"需先解锁：名称1、名称2"（L3432-3433 prereqNames）；③ 新增独立「前置依赖（未满足）」行 `.tn-pre`（朱红警示框，L932 CSS + L3442 定义 + L3448 渲染），仅在 !preOk 且 prereq 非空时显示。仅改 index.html，数据/引擎零回归；Bash 沙箱临时故障未跑 node --check，改动为局部模式清晰片段，已用 Grep 逐处确认语法结构与 prereq id 均有 name。
- 状态：未提交（延续「暂不提交」）。
- 【prereq completeness 审计+修复】陛下指出蒸汽舰只需轮船不需蒸汽机（跨分支缺口），要求全面审计。审计全部 age>=6 节点 1134 的 prereq，按 design pillar「应用技术机理依赖基础学科/底层通用技术则必列前置、同领域代际链式不强加」识别 15 处硬逻辑缺口并补全：蒸汽舰/汽车←蒸汽机、摩托化/装甲骑/高速路←汽车、轮船招商/远洋轮←轮船、铁甲/速射炮←现代冶、水电站←电动机、塑料工/化学药←现代化学、生物工←分子生物、电报谍/激光武←物理学。脚本校验（存在/age<=/去重/无环）后落盘，临时脚本已删；`build_techdata.js` 重建 778.1KB；解析运行期 TECH_TREE 确认 15 条全写入、DFS 无环。仅改 data 两文件，index.html/引擎零回归。备份 tech_tree.json.bak_preaudit_20260808_2230XX。跨分支箭头仍不绘制（分页架构限制，功能判定已生效）。
- 状态：未提交（延续「暂不提交」）。
- 【科技树六维增益显示修正】陛下指出科技树摘要「六维增益」行（清开局≈938/1036/403/1004/592/1364）实属朝代预设 attrBonus，与皇帝初始六维基线重复，无意义。改为「本局六维增益」，只显示玩家开局后自研/升级（state.playerTechs）增量。做法：新增 `playerAttrBonus()`（index.html≈L3319）= `TECH_ENGINE.compute(全量 ownedTechs).attrBonus − TECH_ENGINE.compute(仅预设 presetFor).attrBonus`（属性增益按节点线性叠加，差值恰为玩家增量），Math.max(0,·) 防舍入负值；renderTechTree 改用并改标签。node 烟测：清预设 attrBonus≈用户报数；研究 3 个 era9 节点后增量=国库+36/民心+37/其余0；开局无自研时为「无」。仅改 index.html，数据/引擎零回归。
- 【事件增益封顶修复】陛下二次指出摘要「事件增益（按科技分支累计）」开局即显示 军事+10/国库+6/民心+6/朝政+6/健康+6（清预设把 +10/+6 封顶顶满，研究再无边际收益），同属「含预设」重复计。改为「仅计本局自研」：`branchTechLevels()`（≈L4127）与 `collectBranchTechs()`（≈L4146）数据源由 `state.techProfile.perBranch`(全量=预设+玩家) 改为 `state.playerTechs`（玩家自研），归因「主力」列表同步只列自研项；摘要标签加「·仅计本局自研」，空时显「暂无（研究科技后于对应领域事件生效）」。影响面：显示(techDomainBonus→L5761)、事件注入(injectTechAttrBonus→L4861/4954)、叙事(techBonusNarrative)。**刻意不动 `militaryTechLevel()`（L4138，战争强度/军备广度/warWinChance 仍计全量预设，先进朝代军队本就该强）**。node 烟测：开局空=全0；自研 com5/mil4/agri1 → 国库+2/军事+2/其余0，需大量自研才触顶，研究有意义。index.html 主脚本 `node --check` 退出码 0。仅改 index.html，数据/引擎零回归。


## 六维属性 tooltip 被进度条遮挡（本轮：陛下反馈「悬停六维描述被进度条遮挡」）
- 现象：鼠标悬停六维任一条，弹出的属性释疑说明被下方进度条/事件面板遮挡。
- 根因（CSS）：`.stat`（国库/民心/军事/朝政/健康 五个维度）**没有 `position:relative`**，仅 `.stat.tech-stat` 有；而 tooltip 伪元素 `::after` 是 `position:absolute; top:calc(100% + 8px)`，定位基准取最近的已定位祖先 = 整个 `.stats` 网格容器。`top:100%` 等于**整个六维网格的高度**，故悬停任意维度时说明框都锚到网格最底部，压在下方事件面板/进度条上而被遮挡。
- 修法（仅 `index.html` CSS，未动 JS）：① 给 `.stat` 加 `position:relative`，让 tooltip 锚定到**被悬停的那一条**；② 把 tooltip 改为显示在**该条上方**（`top:calc(100%+8px)`→`bottom:calc(100%+8px)`），指向小三角同步翻为向下（`border-top-color`）；③ `z-index:50`→`1000`，确保浮于顶栏(z-index:50)与进度条之上不被遮挡。④ 顺手修掉 tech-stat 悬停时残留的悬空指向箭头（`.stat.tech-stat:hover::before` 一并 `opacity:0`，原规则只藏了 `::after`）。
- 验证：改动落地后两段 CSS 确认生效（锚定+上方+z1000）；未触碰 JS 与数据层，存档兼容。
- 状态：未提交（延续「暂不提交」）。


## 科技树 occ 分支重构：方技玄术 → 格物穷理（本轮：陛下要求）
- 背景：陛下明确指出「方法玄术的科技树应该优化，前期要体现迷信逐渐破除的过程，后期要体现先进哲学、思想、方法论方面进行优化」。经确认采用「全面重设为哲学/思想/方法论」。
- 分支重定义（tools/gen_techtree.py BRANCHES）：`occ` 由「方技玄术」(attrs=[tech,health], tracks=炼丹/卜筮/风水/方技/堪舆/符箓/星命/遁甲/巫蛊) 改为「格物穷理」(attrs=[court,tech], tracks=宇宙观/认知逻辑/政治哲学/伦理道德/理性启蒙/史学考据/科学方法/社会思想/系统辩证)。分支 id `occ` 不变，故 data/emperors/*.json 中预置 `occ_*` 节点 id 全部仍有效，无需改皇帝预设。
- OCC 矩阵重写（126 节点，14 age × 9 track）：每条轨道从「原始/迷信 → 理性 → 先进哲学·思想·方法论」演进。第 5 轨「理性启蒙」为**破除迷信主轴**：巫觋通神→卜筮决疑→淫祀怪力→疾虚妄(论衡)→神灭论(范缜)→疑经辨伪→西学格致→破除迷信→科学精神→理性批判→证据主义→怀疑方法→认知自觉→元理性。
- SPECIAL_TO_ATTR（9 新键全部→court/tech，契合分支 attrs）：cosmos/logic/poliphil/ethics/enlighten/historio/scimethod/social/systemic。
- SPECIAL_DESC（9 新键一句话介绍）同步重写。
- FUNC_DESC（tools/tech_funcdesc_sequences.py）：用 _rewrite_funcdesc.py 按位替换 9 旧键→9 新键（旧 alchemy→cosmos, divin→logic, fengshui→poliphil, fangji→ethics, kanYu→enlighten, talisman→historio, fate→scimethod, dunjia→social, witch→systemic），每条 14 句 ‖ 演化叙事。
- EFFECTS（tools/add_tech_effects.py）：occ 段 9 键重命名（短码序列沿用旧平衡，量级不变）。解锁标签 `U:占断`→`U:实证`（crisis 域）、`U:占验`→`U:统观`（any 域）。
- index.html：UNLOCK_DOMAIN 占断→实证、占验→统观；injectTechUnlocks 内两处抉择文案改写（「召方士卜筮…占断」→「命儒臣格物实证…实证」；「仰观天象…占验」→「仰观宇宙、统观万化…统观」）；注释同步。
- docs/科技树系统设计.md L69 分支表：`|occ|方技玄术|科技/健康|炼丹·卜筮·…|` → `|occ|格物穷理|朝政/科技|宇宙观·认知逻辑·…|`。
- 重新生成管线：gen_techtree.py → add_tech_effects.py（自检 81 线×14 段相邻 age 主类型全不同、无 score 残留通过）→ build_techdata.js。产出 data/tech_tree.json(1134 节点)、data/tech_data.js(932.7KB)。
- 验证：occ 节点 126、9 新 special 全在 specialToAttr 且无旧键残留；0 节点缺 specialAttr；0 occ 节点缺 ｜ 分段 functionDesc；unlock 存为 {type:'unlock',target:'实证'/'统观'}，与 index.html UNLOCK_DOMAIN 一致；占断 0 处、占验仅存于节点名「占验推类」(age2 科学方法·早期推理概念，非旧解锁标签)；tech_data.js `node --check` 退出码 0。
- 状态：未提交（延续「暂不提交」）。改动 tools/gen_techtree.py / tools/add_tech_effects.py / tools/tech_funcdesc_sequences.py / data/tech_tree.json / data/tech_data.js / index.html / docs/科技树系统设计.md。备份 tech_tree.json.bak_effects_20260808_231559。
