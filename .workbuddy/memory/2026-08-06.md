# 2026-08-06 工作日志

## 技术美术优化（择决千秋 / Choice-of-History）
- 审计结论：纯 Web（原生 JS+HTML+CSS+Canvas），全仓零位图，美术=100% CSS 渐变+Canvas 水墨。填补了《游戏体验审查报告》《优化设计文档》明确排除的"美术表现"空白。
- 关键发现（源码验证）：
  - P0 字体 CDN 依赖（index.html:7-9）破坏离线双击美术；P0 水墨转场 Canvas 无 DPR 缩放（:1041-1042）导致 retina 模糊。
  - P1：18 粒子常驻无限动画无 reduced-motion 守卫、backdrop-filter 缺 -webkit- 前缀、9 处 transition:all 反模式、--dim-light 对比度≈3:1 未过 AA、科技树深色皮肤与主界面脱节、科技树 pts 输入无防抖全量重建 DOM。
- 本回合实施 8 项安全零回归修复：转场 Canvas DPR 封顶 2x+CSS 尺寸分离、粒子 12/6+will-change、prefers-reduced-motion 守卫、backdrop-filter 加 -webkit-、transition:all 全改显式属性（残留 0）、--dim-light 改 #6f6151（≈5.3:1）、字体回退栈追加 Songti/SimSun（cursive 残留 0）、科技树 pts 防抖 120ms。
- 产出交付物：`技术美术优化规格书.md`（审计表 + 已实施 + 待拍板 + Web 预算规格 + KPI）。
- 待陛下拍板：P0 字体真自托管（需 woff2 文件，无法离线抓取）、P1 科技树视觉统一方向（A 浅色同源 / B 深色兄弟）。
- 续（陛下指示后）：P0 字体自托管取消（"游戏联网玩，不考虑断网"）；科技树视觉方向定为方案 A 宣纸同源并已落地（tech_tree_ui.html `:root` 改宣纸体系+宋体+分支大地色，残留冷红 `rgba(255,107,107)`→`rgba(139,58,58)`）。
- 回合二（对比度审计）：新建 `tools/contrast_audit.js`，解析两文件 `:root` 色板，对 28 项真实 UI「文本×背景」配对算 WCAG 2.1 对比度，CI 友好（不达标 EXIT=1）。首跑发现 9 处不达标，集中在金/强调色作小号正文（`--gold` #9a7b3f、`--acc` #87692c）。修复：`--gold`→`#7d5e28`、`--acc`→`#7a5a24`（手算保证 bg 上 ≈5.1/5.4:1）；删掉 `--gold-bright` 的假阳性文字配对（其仅作装饰渐变填充）。复跑全过 EXIT=0。规格书 §1/#10、§2、§4、§5、§6 已同步。

## 统一剧情框架（叙事设计师 agent）
- 根因诊断：各月选项散、无连贯，因 `index.html` 的 `PROMPTS.user()` 只喂 5 月标题+六维+anchors+relations，**从不回灌 spine/幕/激活 flags/常驻班底**，AI 每月在真空中即兴。
- 设计三层次统一：史官统一音色 + 每帝 `reignPremise`/四幕弧 + 常驻班底 `cast`；flag 驱动剧情阶梯；AI 提示词仅注入「引力源」（软约束），不硬编码每日事件，保留文字生成与随机性。
- 关键 schema 勘误（已写进 `叙事设计-统一剧情框架.md` §4.2）：`branches[i]` 只带 setFlags/clearFlags/nextEventId；relations 由**兄弟数组** `relationDeltas[i]` 驱动。在 branches 里写 relationDelta 无效、被代码忽略。
- 实施（已落地并重建验证）：
  - `index.html`：PROMPTS 区插入软注入——`FLAG_OMEN` 辞典、`reignProgress()`/`actNameOf()`/`ACT_ATMOSPHERE`/`narrativeContextFor()` 辅助函数；system() 注入「本朝叙事主线索」；user() 注入 `narrativeContextFor(state)`。缺 reignPremise/cast 时返回空串，**零回归**。全文件内联 JS 语法体检通过。
  - `data/emperors/08-qing.json`(qing_taizu)：补 `reignPremise`+`voiceTag:"质朴"`+`cast`(额亦都/舒尔哈齐/龚正陆)；增强既有「努尔哈赤中年之厄」midCrisis（补 id=`qing_taizu_e_zhongni`、5 项 relationDeltas、5 项 branches）。
  - `04-tang-wudai.json`(tang_taizong)：`reignPremise`+`voiceTag:"华赡"`+`cast`(房玄龄/魏徵/长孙无忌/李靖)。
  - `05-song-liao-jin-xixia.json`(song_gaozong)：`reignPremise`+`voiceTag:"冷峻"`+`cast`(岳飞/秦桧/韩世忠/赵鼎)。
  - 已重跑 `tools/build_data.js` 重建 `coh_data.js`，经 Node 校验三位皇帝 spine/cast 与努尔哈赤 midCrisis 全部落点正确。
- 交付物：`叙事设计-统一剧情框架.md`、`docs/努尔哈赤剧情脊柱示例.json`。
- 修复一个 JSON 重复键 bug：qing_taizu 既有的 midCrisisAnchors（努尔哈赤中年之厄，仅4字段）被我误插第二个同名键，JSON.parse 留后者、丢弃前者 → 改为增强既有事件，已验证。
- 待扩展（P3）：把 spine/cast 铺到更多皇帝。

## 史官音色 Pillars（P1 已落地，index.html）
- 新增 `HISTORIAN_VOICE`（通用拟史官体·记—指—悬三段式）+ `VOICE_TAG_VARIANT`（质朴/华赡/冷峻）配置；`historianVoiceFor(emp)` 注入 `PROMPTS.system` 的【本朝叙事主线索】作 AI 软约束。
- 每月 `historianComment`（史官评）提示词强化为"记—指—悬"三段式，仍由 AI 生成（保留文字生成与随机性，契合陛下"不要全固定"）。
- 三类话术骨架落地：① `historianPrologueFor(emp)` 登基定场白→`selectEmperor` 注入 intro 屏 `#intro-verdict`；② 每月史官评（AI）；③ `historianEpilogueFor(emp,ending)` 结局判词→`endGame` 追加 `#end-review`（加 white-space:pre-line 换行）。
- 零回归：emperor 缺 reignPremise/voiceTag 时全部跳过，259 帝旧行为不变。全文件内联 JS 语法体检通过；三位已铺 spine 皇帝的话术输出已 Node 实跑验证。

## 随机时局系统（v0.2）接入 index.html（已落地）
- 用户指令：直接把 eraTag + RANDOM_POOLS + enterMonth 掷骰落进 index.html，随机事件 ≥500 种且丰富多样；方向=无抉择自动改数值（v0.2）。
- 数据：`tools/gen_random_pools.py` 生成 `data/random_pools.js`，**8 桶共 507 类**自动生效外生冲击；各桶失败链四维（T/P/M/C）净漂移校验全部 OK（court 正偏 / health 负偏按设计可接受，不报警）。
- 时期分桶 `eraTagOf(emperor)`：按 `reignStart` 史料年份映射 8 桶（边界年 960/1271/1368/1644 用开区间 `<`，立国年归新朝桶；1644 含顺治入关归清），**零改动 262 帝数据**，全部分桶验证通过。
- index.html 改动（已落地）：① 新增 `data/random_pools.js` 脚本标签（缺失则 `canFireRandom` 优雅降级返回 false）；② 常量 `P_RAND=0.18`/`RANDOM_COOLDOWN=1`/`ANTI_CLUSTER=0.6`/`RANDOM_STAT_CAP=OUTCOME_LIMIT_NORMAL`(±10)/`RANDOM_STAT_CAP_MAJOR=OUTCOME_LIMIT_CRISIS`(±20)，复用 B2 单一真相；③ `newState` 加 `_lastRandomId`/`_monthSinceRandom`/`_lastRandomMajorNeg`；④ `enterMonth` 非危机月、科技阻力后、中段加压前插入 `随机时局` 掷骰 + 底部早分发 `fireRandomEvent()`；⑤ 函数簇 `eraTagOf`/`reignPhase`/`canFireRandom`/`weightedPick`/`loadRandomEvent`/`scaleByResilience`/`applyRandomChanges`/`fireRandomEvent`/`renderRandomEvent`。
- 设计要点：随机事件复用 B1 失败链（nextMonth 结算）与 `applyCoupling`；**刻意不含 applyOutcome 安全网**（+2 保底），允许纯负面冲击；渲染复用 `renderOutcome`，呈现"事件叙事 + 变化面板 + 推进下月"，无选项。
- 校验（Node 隔离沙箱，抽取真实函数块+真实数据）：`scaleByResilience` 公式正确（满值×0.7/中位×1.0/零值×1.3）；`applyRandomChanges` 硬上限锁死（非 major≤±10、major≤±20）；8 桶×600 月长局模拟命中率≈17.3%、calamity/boon 均衡、相邻同 id 重复=0/NaN=0/越界=0/连续 major 负面最长=1（反簇集生效）；`index.html` 主脚本 `node --check` 通过。
- GDD `docs/随机事件系统设计.md` 新增 §13 已落地状态（含验收结论与待 playtest 项）。
- 待 playtest 项（[PLACEHOLDER]）：真实浏览器 DOM 表现、`P_RAND=0.18` 在完整对局中"意外崩盘占比"（目标<总崩盘15%）需 Monte Carlo/实测回填。
