# 《抉择千秋》（Choice of History）游玩体验审查报告

> 审查视角：以最严苛的游戏用户体验（Game UX）标准，仅针对**游玩体验层面**做系统化梳理。
> 审查边界（明确排除）：本报告不讨论任何美术表现、视觉风格、字体、配色、版式等画面相关问题；所有结论均围绕机制、系统、反馈、节奏、功能与可玩性。
> 审查方法：通读 `index.html` 全部运行逻辑（数据加载、月循环、AI 调用、选择/后果、危机、结算、存档、设置）与 `data/emperors/*.json` 数据结构，并对硬编码甲类皇帝的事件密度做了量化统计。**未做浏览器实机试玩**（受运行环境所限，游戏依赖外部 API 与浏览器），结论基于源码审计 + 数据量化，凡涉及实机表现处均已标注。

---

## 〇、核心结论速览

| 维度 | 体验健康度 | 最严重问题 |
|------|-----------|-----------|
| 历史真实感 | 🔴 严重 | 真实史实仅占游玩时长 ~2.7%，体验被 AI 填充主导 |
| 玩家自由度 | 🔴 严重 | 大事件强制按年触发，决策无法改变历史走向 |
| 游戏创新程度 | 🟠 不足 | 核心创新"科技穿越/保守派阻力"仅为一句提示，未落地 |
| 交互反馈 | 🟠 不足 | 预设事件倾向标签错位，核心决策信号失真 |
| 节奏与难度曲线 | 🔴 严重 | 每月双 AI 往返、最长可达 60s 等待；难度曲线扁平 |
| 性能流畅度 | 🔴 严重 | `file://` 下 fetch 被拦截，多数朝代/皇帝静默丢失 |
| 功能可用性 | 🔴 严重 | "双击即玩"承诺与 fetch 依赖自相矛盾 |
| 系统设计合理性 | 🟠 不足 | 终局评级把"健康"计入治国评分，长寿即可拉高均值 |

---

## 一、历史真实感（Historical Authenticity）

### 问题 1.1（P0）"历史为骨"在游玩体验中被倒置为"AI 为肉"
`enterMonth()` 每月按 `year` 命中预设史实事件，否则进入"日常政务"由 AI 即时生成。对硬编码的 18 位甲类皇帝统计：

- 18 位甲类皇帝合计 **6792 个月**，其中真实史实事件仅 **182 则**，**平均占比 2.68%**。
- 即平均每位皇帝约 **37 个月**才遇到 1 则真实史实事件；最短的乾隆/康熙在位 62 年（744 月）仅 10–12 则史实，占比低至 **1.3%–1.6%**。
- 乙类皇帝事件更少（多数 5–8 则），填充比例只会更高。

**后果**：玩家绝大部分时间在处理 AI 凭 `eraContext` 临时编造的"内政/灾异/边烽"小事件，而非该皇帝的真实传记。游戏最核心的卖点（扮演真实帝王、经历真实历史）在体验层面被稀释成背景板。

**可落地改进**：
1. 把"日常政务"从纯 AI 编造改为**史材参数化抽取**：将《本纪》《资治通鉴》系年条目按朝代结构化（年份 + 真实事件类型 + 真实人物），按月密度从素材库抽取，AI 只负责"在该真实条目下生成 5 个契合时代、差异化的选项与后果"，而非凭空造事件。
2. 提高预设密度下限：关键皇帝至少每 1–2 年 1 则，普通皇帝每 2–3 年 1 则；用"真实小事件"填补而非"AI 大事件"。
3. 引入 `eventSource` 标注，结算时展示"你亲历的真实史实 X 则 / 体验到的虚构政务 Y 则"，把真实感做成可感知的指标。

### 问题 1.2（P0）大事件强制按年触发，与玩家状态无关，"改变历史走向"落空
`enterMonth()` 中史实事件仅由 `ev.year === state.year && !triggered` 决定，**完全不看当前数值、不看前序选择**。靖康之变、安史之乱、马嵬之变等既定在编年表中按年弹出，玩家只能在其中"选一种反应"。

**后果**：设计文档承诺"玩家决策可改变历史走向"，但实现里玩家并无改变事件是否发生、以何种形态发生的权力；自由度的天花板极低，"改变历史"只是数值层面的调味。

**可落地改进**：
1. 让事件的**发生/形态**受状态门控：例如民心 < 阈值则民变提前、军事弱则边患升级；提供真正"规避/推迟/转移"的选项并改写 `triggeredEvents` 与后续序列。
2. 引入 `state.flags` 分支旗标：玩家关键选择写入旗标，后续事件按旗标改写标题、选项与后果，形成结构性分歧而非仅数值浮动。

### 问题 1.3（P1）预设事件的"倾向标签"按选项序号机械分配，与真实语义错位
`fallbackEvent()` 对预设史实事件调用 `hintFor(i, historical)`，把 `["进取·冒险","保守·稳重","革新·远图","怀柔·安抚","铁腕·果决"]` 按**下标**套到 5 个选项上。但预设史实事件的 5 个选项通常是"同一处境下的五种具体应对"（如荆轲刺秦：搏杀 / 走避 / 召武士 / 许地缓兵 / 先杀秦舞阳），根本不是按进取—怀柔—铁腕排列的。

**后果**：选项上的倾向暗示经常是**错的**（"怀柔·安抚"可能贴在"先杀秦舞阳以儆"这种铁腕选项上），直接污染玩家权衡决策时最依赖的信号，也违背了"不能一眼分辨好坏"的设计硬要求。

**可落地改进**：预设事件的每个选项在数据里**自带 `hint` 字段**（由史料编写者标注真实倾向），不再用 `hintFor` 的序号模板；仅 AI/后备事件才用启发式标签。

### 问题 1.4（P2）史实性仅靠提示词约束，无内容级校验
数值变化有 `clamp(±15/±25)` 硬截断，但对"是否穿越、是否张冠李戴"无任何校验——全靠 AI 自觉。离线/后备模式干脆完全不校验。

**可落地改进**：为每则事件附带 `eraGuard`（允许的官职/器物/人物白名单或时代上限），生成后做轻量关键词/实体校验，命中违禁项则降级到后备；对日常事件同样纳入时代约束检查。

### 问题 1.5（P2）史实选项恒为第 0 项且按钮直标"史实之择"
多数预设事件 `historicalChoice = 0`，且 `hintFor` 会把该按钮标成"史实之择"。这既是**剧透**（直接给出"标准答案"），又削弱"难辨优劣"的权衡乐趣，还让历史走向的"正确解"被显式标注。

**可落地改进**：默认隐藏"史实之择"标签（或仅在结算对比页展示"你的选择 vs 历史实际"），把历史对照留到终局回顾，而非决策当场剧透。

---

## 二、玩家自由度（Player Freedom）

### 问题 2.1（P0）选择只改变 6 项数值，无分支叙事、无连锁因果
所有决策的后果最终都收敛为 6 个数值的加减（`applyOutcome`）。所谓"前序选择对后续产生关键或累积影响"在实现里**只有数值累积**，没有任何结构性分歧——没有因为"你早年诛杀功臣"而触发"功臣子弟复仇"这类连锁事件。

**可落地改进**：建立 `flags` / `relations` 系统，关键选择改写后续事件池与可用选项；让抉择真正"长牙齿"。

### 问题 2.2（P0）大事件不可规避，自由度天花板低
见 1.2。玩家在既定事变里只能挑一种反应，无法阻止或改写事变本身。

### 问题 2.3（P1）离线/后备模式下后果按选项下标固定映射，自由沦为"按编号刷分"
`fallbackOutcome()` 用固定的 `profile` 数组按 `currentChoiceIndex` 给增减（idx0 必加军事、idx2 必加科技……）。一旦无 AI，玩家可**机械地按编号最大化收益**，自由选择变成透明最优解，彻底破坏"每个选项有得有失"。

**可落地改进**：后备后果应基于**选项语义或事件内置的多分支后果模板**，而非下标；并为预设事件补充 `historicalOutcome` 之外的分支后果文本与数值。

### 问题 2.4（P2）缺乏进阶玩法变量与多结局
除"科技穿越"外，没有隐藏路线、没有多结局分支、没有可探索的系统（如继承人培养、党争派系、经济改革线）。终局只有"明君/庸君/昏君/亡国之主"四档，路径单一。

**可落地改进**：在六维之外引入 1–2 个轻量状态机（如"继承危机""党争烈度"），让不同玩法路线通向不同结局文本与后续事件。

---

## 三、游戏创新程度（Innovation）

### 问题 3.1（P0）核心创新点"科技穿越 / 保守派阻力"仅为一句 toast，未落地
设计文档将"科技可超越时代但触发保守派阻力"列为**核心特色**，但 `renderOutcome()` 里只做了：
```js
if(state.stats.tech > state.emperor.initStats.tech+20){
  toast("科技远超时代，朝臣上疏反对「奇技淫巧」");
}
```
**没有任何数值惩罚、没有专门事件、没有分支**——承诺的机制被架空成一条提示文字。

**可落地改进**：`tech > init+20` 时按概率/累积触发"保守派阻力"预设事件，真实扣减朝政/民心，并提供"打压异见 / 调和鼎鼐 / 激进推行"等真正改变后续走向的分支。

### 问题 3.2（P1）月度五选一 + 史官点评 是成熟品类的常规实现
整体循环（每月一择、数值反馈、史官论赞）属于"帝王模拟 / 卡牌抉择"成熟范式（如 Reigns、各类皇帝模拟），在**结构层面无新意**；差异化卖点（穿越科技、史官体例）又未做实。

### 问题 3.3（P2）史官点评与数值曲线是点缀，未形成独到的系统循环
点评是 flavour text，曲线是结算装饰，二者都没有反馈回玩法循环（例如点评风格随玩家取向变化、曲线异常触发特殊事件）。

---

## 四、交互反馈（Interaction Feedback）

### 问题 4.1（P1）预设事件倾向标签错位（同 1.3）
这是反馈层最直接影响决策信噪比的问题，详见 1.3——核心决策信号（倾向暗示）在预设事件上失真。

### 问题 4.2（P0）每月两次顺序 AI 调用、两次加载、无步骤提示与跳过
`loadEvent()` 先调 AI 生成事件，`makeChoice()` 再调 AI 生成后果，两次串行、各最多 30s 超时。玩家每月面对**两次 loading**（"正在生成本月事件"→"正在判择定果"），无进度区分、无"跳过动画/快速模式"、无后果预览。

**可落地改进**：
1. 事件生成时**一并生成各选项的后果预览**（或合并为单次调用），把两次等待压成一次。
2. 后台**预取下月事件**，玩家做选择时后果已就绪。
3. 提供"加速/自动"档与键盘 `1–5` 选择（见 4.3）。

### 问题 4.3（P1）无键盘操作，仅能鼠标点按
`renderEvent()` 的选项全是 `<button onclick>`，不支持数字键、不支持回车确认、长抉择列表无快捷键。长在位（数百月）下操作疲劳明显。

**可落地改进**：监听 `1–5` / 方向键 + 回车；设置里提供"自动连点/智能建议"。

### 问题 4.4（P2）科技阻力 toast 反复刷屏
只要 `tech > init+20`，**每个月**都会弹同一条 toast（无去重、无冷却），形成提示噪音。

**可落地改进**：改为一次性机制触发或带冷却/计数，避免重复骚扰。

### 问题 4.5（P2）数值变化反馈良好，但缺"归因"
`renderOutcome()` 的 `+/-` 数值 chip 与颜色反馈清晰（这是做得好的部分）。但玩家看不到"为什么这项变了这么多"，缺乏把叙事与数值勾连的轻量解释。

---

## 五、节奏与难度曲线（Pacing & Difficulty）

### 问题 5.1（P0）回合等待过长，长在位体验冗长
双 AI 往返使单回合理论等待可达 **60s**；康熙/乾隆等 744 月的在位，若全程硬玩将是数百次等待。即便有自动存档，节奏感极差。

**可落地改进**：同 4.2 的合并调用 + 预取 + 加速模式；并对长在位提供"年度快进/关键节点才停"的游玩模式。

### 问题 5.2（P0）难度曲线扁平，缺乏张力
- 危机阈值低至 `≤10`，且数值变化被 `clamp(±15)` 限制，配合 `avg` 驱动的 `健康 ±1/-2`，前期几乎不会崩盘。
- 没有随在位时长/历史节点递增的事务难度，没有"中年危机/晚年衰减"的加压节奏。
- 预设事件稀疏且等距触发，缺乏"高潮—缓冲"的叙事呼吸。

**可落地改进**：
1. 危机阈值随时代/在位阶段动态调整；引入"连低加速崩盘"（已有 `lowStreak` 但未充分使用）。
2. 后期事件权重递增（边患、党争、继承危机随年限加重）。
3. 引入自然寿命衰减（健康随年龄/在位年数下滑），给终局收束压力。

### 问题 5.3（P1）无自然结局压力，节奏无收束
`nextMonth()` 中只要 `avg ≥ 60` 每月 +1 健康，理论上可**无限在位**；仅 `year > reignEnd+5` 才 -3。缺少"功成身退/晚年变局"的强制收束，长线游玩易陷入无目标的数值维护。

---

## 六、性能流畅度（Performance）

### 问题 6.1（P0）`file://` 下 `fetch` 被浏览器拦截，乙类皇帝静默丢失
`loadEmperorData()` 用 `fetch("data/emperors/*.json")` 加载乙类皇帝。在 `file://` 协议下，Chrome/Edge 会以 CORS 策略**拦截本地 fetch**（"Cross origin requests are only supported for http…"）。`catch` 分支仅 `console.warn`，**UI 无任何提示**。

**后果**：玩家按设计文档"双击即可运行"以本地 `file://` 双击打开时，18 个仅含乙类皇帝的朝代（商、周、三国、晋、南北朝、五代、辽、金、西夏）直接**不显示**（被 `d.emperors.length` 过滤），"夏至清全覆盖"承诺在最简安装路径下破功。需注意：**已部署到 HTTPS（如 GitHub Pages）的版本因 fetch 走 http(s) 而正常**，此缺陷仅针对"本地双击文件"这一设计文档声称的支持方式——即承诺与实现在该路径上冲突，而非线上部署不可用。详见七。

**可落地改进**：
1. 构建期把 JSON **内联进 `index.html`**（或改用 `<script src>` 注入全局变量而非 `fetch`），彻底规避 `file://` CORS。
2. 若保留 fetch，启动时检测失败并**在 UI 明确提示**"检测到本地文件读取受限，请通过本地服务器打开，或以下朝代暂不可用"，而非静默消失。

### 问题 6.2（P1）每回合网络往返是主要卡顿源
AI 调用不可缓存、不可预取（当前实现），是流畅度瓶颈。即便网络良好，串行两次调用也造成明显空窗。

**可落地改进**：同 4.2——合并调用、预取、对重复/相似情境做轻量缓存。

### 问题 6.3（P2）无增量/懒加载
135KB 单文件 + 全量数据内嵌，首屏尚可；但 8 个 JSON 为启动时顺序 `await`（`for...of` 串行），虽总量不大，仍可在进入菜单前做并行 `Promise.all` 优化。

---

## 七、功能可用性（Feature Usability）

### 问题 7.1（P0）"双击即玩"承诺与 fetch 依赖自相矛盾
设计文档明确写"无构建步骤，双击即可运行"，但运行却依赖 `fetch` 本地文件——在最常见（双击）的打开方式下功能残缺且无报错。这是**承诺与实现的直接冲突**。

### 问题 7.2（P0）内置加密凭据随包分发，存在单点故障与滥用风险
`BUILTIN_CRED_CIPHER` 把某个第三方端点（`https://api.agnes-ai.cn/v1`）的 API Key 以"混淆级"密文随包下发，默认 `builtin:true`，所有未自行配置的玩家共用同一把钥匙：
- 第三方端点/密钥一旦失效或限流，**全体默认玩家直接无法生成事件**（退化到极小后备池）。
- "混淆"并非安全，密钥可被还原，存在被滥用的风险。
- 玩家对"自己的请求发往何处、花谁的钱"**不知情**。

**可落地改进**：
1. 默认**不内置第三方凭据**；首次启动引导玩家填入自己的 API（明确告知用途、费用、数据去向）。
2. 提供明确的"离线模式"入口：进入纯后备事件体验，并诚实告知"内容有限、非 AI 生成"。

### 问题 7.3（P1）JSON 解析失败仅 console.warn，整文件皇帝静默消失
`loadEmperorData()` 单个文件解析异常只 `console.warn`，该文件全部皇帝无声丢失，玩家无从得知"少了一整个朝代"。

**可落地改进**：失败时在菜单/日志区给出可见提示，并标注缺失的朝代与数量。

### 问题 7.4（P1）读档与运行期注册表强耦合，易断裂
`doLoad()` 通过 `emperorId` 在 `EMPEROR_REGISTRY` 中查找。若乙类皇帝因 6.1 的 fetch 失败而不在注册表，读取其存档会报"找不到皇帝数据"，体验断裂。

**可落地改进**：存档内**内联皇帝数据快照**（至少背景、初始数值、事件库引用），降低对运行期注册表的依赖（亦缓解 6.1 的连锁影响）。

### 问题 7.5（P2）存档能力单薄
仅 3 手动 + 1 自动槽，自动槽每月覆盖；无导出/导入、无云同步、无多版本对比。对长线游玩（数百月）的容灾偏弱。

---

## 八、系统设计合理性（System Design Rationality）

### 问题 8.1（P0）终局评级把"健康"计入治国评分，长寿即可拉高均值
`endGame()` 的 verdict 用六维**算术平均**，`avg ≥ 70=明君`。但"健康"是皇帝身体状态，与治国优劣弱相关；一个靠数值维护活得久、却把国家搞得一团糟的玩家，可能因健康满值拉高均值而被判"明君"。评级与玩法真实质量错位。

**可落地改进**：
1. 终局评分拆为"治国均值（排除健康）"为主，"健康/寿命"单独成项。
2. 引入历史符合度、关键抉择质量等多维加权；对超长在位做边际折扣，避免"苟活即高分"。

### 问题 8.2（P1）六维之间无协同/权衡网络，仅加法平均
国库、民心、军事、朝政、健康、科技彼此独立，没有"高军事拖累国库""高压统治反噬民心"这类联动；科技阻力又被架空（见 3.1）。系统深度停留在"六个独立进度条"。

**可落地改进**：加入少量耦合规则（如军事长期处于高位自然消耗国库；朝政过低放大其他项的负面波动），让数值变化有"牵一发而动全身"的体感。

### 问题 8.3（P1）健康缺乏专门事件驱动
健康变化几乎只来自后果增量与 `avg ±1/-2`，缺少"染疾/操劳/御医"等专门事件；"皇帝身体"作为六维之一却最被动。

### 问题 8.4（P2）危机判定维度不完整
`detectCrisis()` 只判国库/民心/军事/朝政 ≤10，健康单独处理（`≤10` 才提示，但 `=0` 才结束）。前期数值普遍在中高位时，几乎没有任何败因压力，游戏缺乏"输"的逼近感。

### 问题 8.5（P2）存档只存 id，重载依赖注册表完整性
见 7.4，属于设计与功能可用性的交叉缺陷。

---

## 九、优先级路线图（建议落地顺序）

| 优先级 | 问题 | 维度 | 一句话动作 |
|--------|------|------|-----------|
| **P0-1** | 6.1 / 7.1 | 性能/可用性 | 内联或 `<script>` 注入数据，消灭 `file://` 下皇帝静默丢失，兑现"双击即玩" |
| **P0-2** | 1.1 | 历史真实感 | 日常政务改为"史材参数化抽取 + AI 选项生成"，把真实感做回主线 |
| **P0-3** | 1.2 / 2.2 | 自由度 | 大事件的发生/形态受状态与旗标门控，让决策真能改写历史 |
| **P0-4** | 3.1 | 创新 | 落地"科技阻力"真实机制（触发事件 + 数值 + 分支） |
| **P0-5** | 4.2 / 5.1 | 节奏 | 合并/预取 AI 调用，加入速通模式与键盘操作 |
| **P0-6** | 7.2 | 可用性 | 取消默认内置第三方凭据，改引导自填 + 明确离线模式 |
| **P1** | 1.3 / 4.1 | 反馈 | 预设事件选项自带真实 `hint`，废弃序号套模板 |
| **P1** | 8.1 | 系统设计 | 终局评分剥离健康、引入多维加权 |
| **P1** | 5.2 | 难度 | 危机阈值动态化、后期加压、自然寿命衰减 |
| **P2** | 2.3 / 8.2 | 自由度/系统 | 后备后果基于语义、六维加入耦合规则 |
| **P2** | 7.3 / 7.4 | 可用性 | 数据加载失败可见提示、存档内联快照 |
| **P2** | 1.5 / 4.4 | 反馈 | 隐藏当场"史实之择"剧透、科技 toast 去重 |

---

## 十、总体评价（严苛口径）

《抉择千秋》的**史料编写功底扎实**（预设事件考据严谨、史官体例到位），其问题不在"内容有没有"，而在**系统把最好的内容放到了最不显眼的位置、并用脆弱的工程与单调的机制稀释了它**：

- 它把约 97% 的游玩时间交给 AI 临时填充，使"历史为骨"在体验层面名存实亡；
- 它承诺"改变历史走向"，却让所有大事件强制按年发生、玩家只能做反应；
- 它把最有想象力的创新（科技穿越/保守派阻力）做成了一条提示文字；
- 它在最朴素的工程环节（本地文件读取、默认凭据、加载报错）上埋了会直接在"双击打开"这一最常见路径上暴露的可用性雷。

按最严苛的游玩体验标准，当前版本在**历史真实感、玩家自由度、节奏、性能可用性**四个维度存在结构性缺陷，需优先处理 P0 项；在**创新落地、交互反馈、系统深度**上亦有明确可补强的空间。以上所有问题均不涉及画面表现，纯属玩法与系统工程层面。
