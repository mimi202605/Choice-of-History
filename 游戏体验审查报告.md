# 《择决千秋》（Choice of History）游玩体验审查报告（2026-08-05 修订版）

> **审查视角**：以最严苛的游戏用户体验（Game UX）标准，仅针对**游玩体验层面**做系统化梳理。
> **审查边界（明确排除）**：本报告不讨论任何美术表现、视觉风格、字体、配色、版式、动效等画面相关问题；所有结论均围绕机制、系统、反馈、节奏、功能与可玩性。
> **审查方法**：通读 `index.html` 全部运行逻辑（`loadEmperorData` / `enterMonth` / `loadEvent` / `makeChoice` / `applyOutcome` / `fallbackEvent` / `renderPreset` / `nextMonth` / `endGame` / `doLoad` / `callAI` / `saveSettings` 等）与 `data/emperors/coh_data.js`、`*.json` 数据结构，并对全量皇帝数据做了量化统计。**未做浏览器实机试玩**（受运行环境所限，游戏依赖外部 API 与浏览器），结论基于源码审计 + 全量数据量化，凡涉及实机表现处均已标注。
> **重要说明**：本仓库已存在一版早期审查报告，但其结论基于 2026-08-05 **上午的迭代前代码**。当日开发者已通过 `tools/build_data.js`、`fix_01.py`、`_rebuild_g4.py` 等修复脚本大幅改动代码与数据（数据由约 198 帝扩至 262 帝、`coh_data.js` 内联打包、科技阻力事件落地、数值耦合与寿命衰减加入等）。**本报告以 2026-08-05 14:01 的 `index.html`（150KB/1899 行）为基准重写**，对已被修复的旧结论做了更正，并补充了新发现的问题。

---

## 〇、核心结论速览（当前代码状态）

| 维度 | 体验健康度 | 最严重问题 | 相对早期报告的变动 |
|------|-----------|-----------|-------------------|
| 历史真实感 | 🔴 严重 | 真实史实仅占游玩时长 **3.67%**，且 AI 模式下作者编写的历史选项被丢弃 | 旧 P0-1 量化值需更新；新增"AI 丢弃历史选项"发现 |
| 玩家自由度 | 🔴 严重 | 大事件强制按年触发，决策无法改变历史是否发生/以何形态发生 | 未变 |
| 游戏创新程度 | 🟢 已落地→仍偏薄 | "科技阻力"已做成真实事件，但为单一通用模板、缺情境变体 | **旧 P0-4 已修复**，降级为 P2 观察 |
| 交互反馈 | 🟠 改善中 | 倾向标签改关键词推断（仍脆弱）；AI 模式下"史实之择"索引标签语义错位 | 4.3 键盘已加、4.4 toast 已去重（已修复） |
| 节奏与难度曲线 | 🟠 改善中 | 事件生成仍依赖网络往返；难度曲线中段仍扁平 | 5.2/5.3 已加寿命衰减与连低加速（部分修复） |
| 性能流畅度 | 🟢 已修复 | file:// 双击已可用（内置全局注入） | **旧 P0-6.1 已修复** |
| 功能可用性 | 🟠 改善中 | 默认仍内置共享凭据（可逆混淆、单点故障）；数据加载失败提示是死代码 | 7.1 已修复；7.3 仅"设标志未展示" |
| 系统设计合理性 | 🟢 已修复→仍浅 | 评级已剔除健康；数值耦合已加但很浅 | **旧 P0-8.1 已修复** |

---

## 〇·一、相较早期报告已确认修复的项（更正记录）

以下早期报告列为 P0/P1 的问题，经当前代码核实**已解决或显著缓解**，特此更正，避免误导：

1. **P0-6.1 / 7.1（file:// 双击失效）— 已修复**：新增 `tools/build_data.js` 将 8 个 JSON 合并为 `data/emperors/coh_data.js`（`window.COH_BUILTIN_EMPERORS` 全局变量），`index.html` 以经典 `<script src>` 注入（line 370）。`loadEmperorData()` 优先读内置全局，仅当内置缺失才降级 `fetch`。"双击即玩"承诺在本地路径上**已兑现**；线上 HTTPS 部署原本也正常。
2. **P0-3.1（科技阻力仅一句 toast）— 已修复**：新增 `TECH_RESIST_EVENT` 含 5 个分支选项与真实数值后果，`enterMonth()` 通过 `shouldTriggerTechResist()`（科技超出时代基线 20 且距上次触发满一年）触发，机制真实落地。
3. **P0-8.1（健康计入治国评级）— 已修复**：`endGame()` 评级改用 `govKeys=["treasury","people","military","court","tech"]`，**剔除健康**；健康/龙体单独展示。长寿拉高均值的漏洞已堵。
4. **P1-1.3/4.1（倾向标签按序号错配）— 已改善**：废弃按选项下标的 `hintFor`，改为 `hintFromText()` 按选项文本关键词推断倾向。虽仍脆弱（见 1.3 残留），但"进取/怀柔贴反"的硬伤已消除。
5. **P1-4.3（无键盘操作）— 已修复**：`bindKeyboard()` 支持 `1–5` 直接抉择、方向键/Tab 移动焦点、结果阶段 `Enter/空格` 推进下月。
6. **P2-4.4（科技 toast 刷屏）— 已修复**：通过 `state._techToastShown` 仅提示一次。
7. **P2-2.3（后备后果按编号刷分）— 已修复**：`fallbackOutcome()` 改为按 `stanceKey`（倾向语义）映射 `STANCE_PROFILES`，不再按选项下标固定加减。
8. **P1-8.2（六维无耦合）— 已部分修复**：新增 `applyCoupling()`，军事>75 耗国库、朝政<25 动摇民心、国库<20 削弱军力。
9. **P0-5.2/5.3（无寿命衰减/无收束）— 已部分修复**：`nextMonth()` 加入自然寿命衰减（在位 >45 年 -1、>60 年再 -1、`reignEnd+5` 后 -3）与连低加速（`lowStreak>=6` 最低项再 -2）。

---

## 一、历史真实感（Historical Authenticity）

### 问题 1.1（P0）真实史实仍仅占游玩时长的约 **3.67%**
对全量数据（`coh_data.js`，262 位皇帝、1613 则事件）量化：

- 皇帝总数 **262**（A 类 73 / B 类 160 / C 类 29）；事件总数 **1613**；按 `（reignEnd−reignStart+1）×12` 估算总在位月数约 **44004**。
- **真实史实月 / 总月数 = 3.67%**。即玩家约 96% 的游玩时间在 AI 即生成的"日常政务"上，而非该皇帝的真实传记。
- 单帝事件数：中位 **5**、均值 **6.2**、最大 15。最长在位者（如康熙 744 月）仅约 12 则史实，占比低至 **1.6%**。

**后果**：游戏核心卖点"扮演真实帝王、亲历真实历史"在体验层面被稀释成背景板——玩家绝大部分时间在处理 AI 临时编造的内政/灾异/边烽小事件。

**可落地改进**：
1. 把"日常政务"从纯 AI 编造改为**史材参数化抽取**：将《本纪》《资治通鉴》系年条目结构化（年份+真实事件类型+真实人物），按月密度从素材库抽取，AI 只负责"在该真实条目下生成 5 个契合时代、差异化的选项与后果"。
2. 提高预设密度下限：A 类皇帝至少每 1–2 年 1 则，B/C 类每 2–3 年 1 则；用"真实小事件"填补而非"AI 大事件"。
3. 引入 `eventSource` 标注，结算页展示"亲历真实史实 X 则 / 体验虚构政务 Y 则"，把真实感做成可感知指标。

### 问题 1.2（P0）大事件强制按年触发，与玩家状态无关，"改变历史走向"落空
`enterMonth()` 中 `ev.year === state.year && !triggeredEvents.includes(...)` 决定史实事件是否弹出，**完全不看当前数值、不看前序选择**。靖康之变、安史之乱等按编年表按年弹出，玩家只能在其中"选一种反应"。

**可落地改进**：
1. 让事件的**发生/形态**受状态门控（民心<阈值则民变提前、军事弱则边患升级），提供真正"规避/推迟/转移"的选项并改写 `triggeredEvents` 与后续序列。
2. 引入 `state.flags` 分支旗标：关键选择写入旗标，后续事件按旗标改写标题、选项与后果，形成结构性分歧而非仅数值浮动。

### 问题 1.3（P1→残留）`hintFromText` 关键词推断仍脆弱，且无作者自带 `hint`
全量数据中 **0 个选项带 `hint` 字段**（grep `"hint"` = 0）。预设事件的非史实选项一律由 `hintFromText()` 按关键词贴标签。问题：
- 关键词规则有限：如"怀柔·安抚"规则含"罢弊政"，可能把偏保守的选项误标成怀柔；"革新·远图"含"设"，极易误伤。
- **AI 模式下历史事件的"史实之择"标签语义错位**：当 AI 可用时，历史预设事件被送入 `callAI` 由其**重新生成 5 个选项**，`historicalChoice` 索引（恒为 0）被原样贴回——但 AI 生成的"第 0 项"未必是作者定义的那条历史选项，标签指向的是 AI 文本而非历史事实。

**可落地改进**：预设事件每个选项在数据里**自带 `hint` 字段**（由史料编写者标注真实倾向）；仅 AI/后备事件才用 `hintFromText` 启发式。历史事件的 `historicalChoice` 应绑定"选项文本指纹"而非裸索引，避免 AI 改写后标签漂移。

### 问题 1.4（P2）史实性仅靠提示词约束，无内容级校验
数值有 `clamp(±15/±25)` 硬截断，但对"是否穿越、是否张冠李戴"无任何校验——全靠 AI 自觉。离线/后备模式完全不校验。

**可落地改进**：为每则事件附带 `eraGuard`（允许的官职/器物/人物白名单或时代上限），生成后做轻量关键词/实体校验，命中违禁项则降级到后备。

### 问题 1.5（P1→普遍化）"史实之择"剧透在离线/兜底路径 100% 出现
量化发现：**1613 则事件的 `historicalChoice` 全部为 0**。在 `renderPreset()` 中，`i===preset.historicalChoice` 的选项当场被标注"史实之择"。该标注在以下情形出现：
- **离线模式 / AI 调用失败时**（即开发者在设置里推荐的"最适合本地 file:// 单机游玩"的模式）——**每个事件都把"标准答案"贴在选项 0 上**，既剧透又削弱"难辨优劣"的权衡乐趣。
- AI 成功路径不显示该标签（因 `renderEvent` 不读取 `historicalChoice` 做标注），但代价是下一节所述——作者编写的历史选项被整体丢弃。

**可落地改进**：决策当场**默认隐藏"史实之择"**，或仅在结算对比页展示"你的选择 vs 历史实际"；把历史对照留到终局回顾，而非决策当场剧透。

### 新增发现 1.6（P1）AI 模式下作者编写的历史选项被整体丢弃
当 AI 可用且命中历史预设事件时，`loadEvent()` 走 `callAI` 路径，把预设的 `title/description` 连同 `historicalChoice` 索引传给 AI，**由 AI 重新生成 5 个选项**，作者精心考据的 5 个历史向选项被完全替换。结果是：在 AI 模式下，玩家看到的是"真实事件框架 + AI 泛化选项"，反而比离线模式更不"历史"。这与"历史为骨"的定位自相矛盾。

**可落地改进**：AI 模式下应以作者选项为基线，仅做"润色/补差额后果"，而非整体重生成；或在事件库里为每则事件预存 AI 可安全复用的结构化选项。

---

## 二、玩家自由度（Player Freedom）

### 问题 2.1（P0）选择只改变 6 项数值，无分支叙事、无连锁因果
所有决策后果最终收敛为 6 个数值加减（`applyOutcome` / `applyCoupling`）。所谓"前序选择对后续产生关键或累积影响"仅有数值累积，没有任何结构性分歧——没有"早年诛杀功臣→功臣子弟复仇"这类连锁事件。

**可落地改进**：建立 `flags` / `relations` 系统，关键选择改写后续事件池与可用选项，让抉择真正"长牙齿"。

### 问题 2.2（P0）大事件不可规避，自由度天花板低
见 1.2。玩家在既定事变里只能挑一种反应，无法阻止或改写事变本身。

### 问题 2.4（P2）缺乏进阶玩法变量与多结局
除"科技阻力"外，没有隐藏路线、没有多结局分支、没有可探索子系统（继承人培养、党争派系、经济改革线）。终局仍为"明君/庸君/昏君/亡国之主"四档，路径单一（评级已剔除健康，但仍是单维均值分档）。

**可落地改进**：在六维之外引入 1–2 个轻量状态机（如"继承危机""党争烈度"），让不同玩法路线通向不同结局文本与后续事件。

---

## 三、游戏创新程度（Innovation）

### 问题 3.1（旧 P0→已落地，现评级 P2 观察）科技阻力机制已落地，但偏薄
`TECH_RESIST_EVENT` 真实触发并含分支，方向正确。残留问题：
- **单一通用模板**：所有朝代、所有情境共用同一段"奇技淫巧之议"文案，缺乏情境化变体（如宋儒 vs 清保守派措辞应不同）。
- **仅触发一次/年**：`shouldTriggerTechResist()` 满 12 个月冷却，长期高科技玩家每年只遇一次，机制存在感弱。
- 历史选项恒为 -1（无"史实之择"），但"借新法之名行聚敛"等分支未与后续旗标联动。

**可落地改进**：按朝代/情境提供 3–5 套阻力文案变体；将"保守派阻力"做成持续派系压力（朝政/民心按月小幅承压），而非一次性事件；分支选择写入 `flags` 影响后续科举、灾异等事件权重。

### 问题 3.2（P1）月度五选一 + 史官点评属成熟品类常规实现
整体循环（每月一择、数值反馈、史官论赞）属"帝王模拟/卡牌抉择"成熟范式（Reigns、各类皇帝模拟）。差异化卖点（穿越科技、史官体例）已部分做实，但结构层面仍无新意。

### 问题 3.3（P2）史官点评与数值曲线是点缀，未反馈回玩法循环
点评是 flavour text，曲线是结算装饰，二者都没有反馈回玩法循环（如点评风格随玩家取向变化、曲线异常触发特殊事件）。

---

## 四、交互反馈（Interaction Feedback）

### 问题 4.1（P1）预设事件倾向标签脆弱
同 1.3——核心决策信号（倾向暗示）在预设事件上仍依赖关键词推断，存在误标风险。

### 问题 4.2（P0→部分）每月至少一次网络往返，无合并/预取/跳过
- 事件生成（`loadEvent`→`callAI`）每次最多 30s 超时；`makeChoice` 在**非速通**时再调一次 `callAI` 生成后果。速通模式（`settings.fastMode`）仅跳过**后果** AI，事件生成仍依赖网络。
- **"速通"名不副实**：事件生成（每月必走的等待）并未被跳过，长在位（数百月）仍要经历数百次网络等待。
- 两次调用串行、各 30s，无步骤区分、无"跳过动画"、无后果预览。

**可落地改进**：
1. **事件与后果合并为单次调用**：让 AI 一次性返回"事件 + 各选项后果预览"，把两次等待压成一次。
2. 后台**预取下月事件**（玩家做选择时后果已就绪）。
3. 速通模式应同时跳过事件生成（走高质量后备池），或在事件生成成功后再进入"后果即时"模式；并在 UI 明示速通仅加速后果。

### 问题 4.5（P2）数值变化反馈良好，但缺"归因"
`renderOutcome()` 的 `+/-` 数值 chip 与颜色反馈清晰（做得好的部分），但玩家看不到"为什么这项变了这么多"，缺乏把叙事与数值勾连的轻量解释。

### 新增发现 4.6（P2）AI 模式下"史实之择"索引标签语义错位
同 1.3 第二段——历史事件的 `historicalChoice` 索引贴到 AI 重生成的选项上，标签指向与历史事实脱节。

---

## 五、节奏与难度曲线（Pacing & Difficulty）

### 问题 5.1（P0→部分）回合等待仍长，长在位体验冗长
双 AI 往返使单回合理论等待可达 60s（事件 30s + 后果 30s）。康熙/乾隆 744 月的在位，若全程硬玩将是数百次等待。即便有自动存档、速通仅省后果，节奏感仍差。

**可落地改进**：同 4.2 的合并调用 + 预取；并对长在位提供"年度快进/关键节点才停"的游玩模式。

### 问题 5.2（P0→部分）难度曲线中段仍扁平，缺乏张力
- 危机阈值低至 `≤10`，数值变化被 `clamp(±15)` 限制，配合 `avg` 驱动的 `健康 ±1/-2`，前期几乎不会崩盘。
- 已加入的寿命衰减（>45 年、>60 年）与连低加速（`lowStreak>=6`）是正向补充，但**缺少随在位阶段递增的事务难度**（如中年党争、晚年继承危机加压），中段仍偏"数值维护"。
- 预设事件稀疏且等距触发，缺乏"高潮—缓冲"的叙事呼吸。

**可落地改进**：
1. 危机阈值随时代/在位阶段动态调整；强化 `lowStreak` 的崩盘加速。
2. 后期事件权重递增（边患、党争、继承危机随年限加重）。
3. 引入"中年危机/晚年变局"的强制加压节点。

### 问题 5.3（P1→部分）无自然"功成身退"收束
`nextMonth()` 仅 `year > reignEnd+5` 才 -3，且寿命衰减到 0 才结束。缺少"功成身退/晚年变局"的强制收束，长线游玩易陷入无目标数值维护（已缓解但未根治）。

---

## 六、性能流畅度（Performance）

### 问题 6.1（旧 P0→已修复）file:// 双击已可用
见〇·一第 1 条。内置全局注入彻底规避 `fetch` 的 CORS，且 fetch 兜底失败时设置了 `window.__cohDataWarn`（**但该提示实际未展示**，见 7.3）。

### 问题 6.2（P1→部分）AI 调用不可缓存、不可预取
当前实现串行两次调用、不可预取（4.2 已详述）。是流畅度主要瓶颈。

**可落地改进**：合并调用、预取、对重复/相似情境做轻量缓存（同源同情境可复用后果模板）。

### 问题 6.3（P2）兜底 fetch 路径仍串行
若内置全局缺失而走 fetch 兜底，8 个 JSON 用 `for...of` 串行 `await`。因主路径已改为内置全局，此路径仅极端情况触发，优先级低；可改为 `Promise.all` 并行。

---

## 七、功能可用性（Feature Usability）

### 问题 7.1（旧 P0→已修复）"双击即玩"已兑现
见〇·一第 1 条。

### 问题 7.2（旧 P0→降为 P1）默认仍内置共享凭据，可逆"加密"实为混淆
- `BUILTIN_CRED_CIPHER` 把 `api.agnes-ai.cn/v1` 的 API Key 以密文随包下发，默认 `builtin:true`，全体未自配玩家共用同一把钥匙。
- **"加密"是混淆级**：`COH_CIPHER` 为 XOR 流密码 + base64，**密钥 K 数组内嵌于源码**（`index.html` line 1068），可被任意还原；并非安全加密。
- 第三方端点/密钥一旦失效或限流，**全体默认玩家直接无法生成事件**（退化到极小后备池）——单点故障。
- 玩家对"自己的请求发往 `api.agnes-ai.cn`、花谁的钱、数据去向"**不知情**（设置页仅称"内置加密凭据"，未披露端点与费用归属）。

**已缓解但未根治**：设置页提供"清空即切换自有接口"与"离线模式"入口，并附测试连接；但**默认仍共用密钥**，SPOF 与知情缺失仍在。

**可落地改进**：
1. 默认**不内置第三方凭据**；首次启动引导玩家填入自己的 API（明确告知用途、费用、数据去向）。
2. 若保留内置，须明示端点与费用归属，并提供一键"离线模式"醒目入口与诚实的内容有限告知。

### 问题 7.3（P1→名存实亡）数据加载失败提示是死代码
`loadEmperorData()` 在 fetch 兜底失败时仅 `window.__cohDataWarn = msg`（line 914，注释写"供 UI 可见提示"），**但全文件无任何代码读取/展示该变量**。即"部分朝代数据未能加载"的警告从未真正出现在 UI 上，玩家仍对缺失一无所知。

**可落地改进**：在菜单/日志区读取并展示 `window.__cohDataWarn`；或直接在 `renderDynasties()` 处对空朝代给出可见标注。

### 问题 7.4（P1→降级为 P2）读档与运行期注册表强耦合
`doLoad()` 通过 `emperorId` 在 `EMPEROR_REGISTRY` 中查找。当前因内置全局恒在，`file://` 下已不易断裂，但设计上若运行期注册表缺失对应皇帝，读取其存档仍报"找不到皇帝数据"。

**可落地改进**：存档内**内联皇帝数据快照**（至少背景、初始数值、事件库引用），降低对运行期注册表的依赖。

### 问题 7.5（P2）存档能力单薄
仅 3 手动 + 1 自动槽，自动槽每月覆盖；无导出/导入、无云同步、无多版本对比。对长线游玩（数百月）容灾偏弱。

---

## 八、系统设计合理性（System Design Rationality）

### 问题 8.1（旧 P0→已修复）终局评级已剔除健康
见〇·一第 3 条。

### 问题 8.2（P1→部分修复）六维耦合很浅
`applyCoupling()` 仅 3 条线性规则（军事>75 耗财、朝政<25 摇民、国库<20 削军）。无"高军事长期拖累国库""高压统治反噬民心"等更丰富的联动。系统深度仍停留在"六个进度条 + 轻耦合"。

**可落地改进**：加入少量耦合规则让数值变化有"牵一发而动全身"的体感（如朝政过低放大其他项的负面波动、科技过高持续触发阻力）。

### 问题 8.3（P1）健康缺乏专门事件驱动
健康变化几乎只来自后果增量、`avg ±1/-2`、寿命衰减，缺少"染疾/操劳/御医"等专门事件；"皇帝身体"作为六维之一仍最被动。

### 问题 8.4（P2）危机判定维度不完整
`detectCrisis()` 只判国库/民心/军事/朝政 `≤10`，健康单独处理（`≤10` 才提示，但 `=0` 才结束）。前期数值普遍中高位时几乎无败因压力，游戏缺乏"输"的逼近感（`lowStreak` 部分缓解）。

### 问题 8.5（P2）存档只存 id，重载依赖注册表完整性
见 7.4，设计与功能可用性的交叉缺陷。

---

## 九、优先级路线图（建议落地顺序，基于当前代码）

| 优先级 | 问题 | 维度 | 一句话动作 |
|--------|------|------|-----------|
| **P0-1** | 1.1 / 1.6 | 历史真实感 | 日常政务改"史材参数化抽取"；AI 模式保留作者历史选项而非整体重生成 |
| **P0-2** | 1.2 / 2.2 | 自由度 | 大事件的发生/形态受状态与旗标门控，让决策真能改写历史 |
| **P0-3** | 4.2 / 5.1 | 节奏 | 合并事件+后果为单次调用、预取下月、速通真正跳过事件生成 |
| **P0-4** | 2.1 / 2.4 | 自由度 | 建 `flags/relations` 连锁因果 + 轻量子系统（党争/继承）与多结局 |
| **P1** | 7.2 | 可用性 | 默认不内置共享密钥；披露端点/费用；醒目离线入口 |
| **P1** | 7.3 | 可用性 | 真正展示数据加载失败提示（读 `__cohDataWarn`） |
| **P1** | 1.3 / 1.5 / 4.6 | 反馈 | 选项自带 `hint`；决策当场隐藏"史实之择"，历史对照留到结算 |
| **P1** | 5.2 | 难度 | 危机阈值动态化、后期加压、中年/晚年变局节点 |
| **P2** | 3.1 | 创新 | 科技阻力情境化变体 + 持续派系压力 |
| **P2** | 8.2 / 8.3 | 系统 | 加深数值耦合、健康专门事件 |
| **P2** | 7.4 / 7.5 / 8.5 | 可用性 | 存档内联快照、增加导出/导入 |
| **P2** | 1.4 | 真实感 | 引入 `eraGuard` 实体校验 |

---

## 十、总体评价（严苛口径）

《择决千秋》在 **2026-08-05 当日已完成一轮重要工程与机制补强**：数据内联打包兑现了"双击即玩"、科技阻力机制真实落地、终局评级剔除健康、数值耦合与寿命衰减补足了难度曲线、键盘操作与 toast 去重改善了交互。上一版报告所列 P0 中，性能可用性、评级合理性、创新落地三项已实质性修复——这是应当明确肯定的进度。

但按最严苛的游玩体验标准，**结构性骨架问题仍未动摇**：

- **历史真实感（最核心卖点）依旧名存实亡**：玩家 96% 的时间在 AI 临时填充的政务上，真实史实仅占 3.67%；更讽刺的是，AI 模式下作者考据的历史选项被整体丢弃、换成泛化选项，离线模式下又 100% 把"史实之择"贴到选项上剧透——两种模式都没把"历史为骨"做扎实。
- **玩家自由度天花板极低**：所有大事件强制按年发生、玩家只能反应，决策无法改写历史是否发生或以何形态发生；选择只改 6 个数值，无连锁因果、无多结局。
- **节奏仍被网络往返卡住**：事件生成每月必等，速通模式名不副实。
- **功能可用性仍有隐患**：默认内置共享可逆混淆密钥（单点故障 + 知情缺失），且数据加载失败的"可见提示"是死代码。

一句话：**它把最好的史料内容放到了最不显眼的位置，并用仍脆弱的工程与单调的机制稀释了它**——修复了"能不能跑起来"与"评分公不公平"的表层问题后，下一阶段必须攻坚"历史是否真被亲历"与"选择是否真有重量"这两个体验命门。以上所有问题均不涉及画面表现，纯属玩法与系统工程层面。

---

## 十一、优化实施记录（2026-08-05 第二轮，基于本报告）

> 依据本报告路线图，在 `index.html` 落地了下列高价值、低风险改动（已通过 Node 语法编译与引用一致性校验；**未经浏览器实机试玩**）。

| 报告项 | 改动 | 代码位置 |
|--------|------|----------|
| **P0-3 / 4.2 / 5.1 节奏** | **合并 AI 调用**：`loadEvent` 改为单次调用同时返回「事件 + 5 选项 + 5 后果」，消除每月第二次网络等待；`makeChoice` 命中预生成 `outcomes` 时直接结算，不再发第二次请求。 | `loadEvent` / `makeChoice` / `PROMPTS.user` / `buildHistoricalEvent` |
| **P0-3 速通名不副实** | `fastMode` 现于 `loadEvent` 即跳过事件生成 AI，完全走本地预设/后备池，实现"秒开"。 | `loadEvent` 条件 `!aiAvailable() \|\| settings.fastMode` |
| **1.6 历史真实感** | AI 模式下不再整体重生成历史选项：历史预设事件**保留作者编写的 5 个选项**，仅让 AI 为既定选项生成后果（`buildHistoricalEvent`）。 | `loadEvent` 历史分支 |
| **1.5 剧透** | 决策当场**不再显示"史实之择"**标签（`renderPreset` 与 `buildHistoricalEvent` 均去除）；内部保留 `historicalChoice`。 | `renderPreset` / `buildHistoricalEvent` |
| **1.5 终局对照** | 终局回顾页新增"你的选择 vs 历史原择"对照（契合史实 / 史实原择：第X项）。 | `endGame` 决策渲染 + `recordHistory` 存 `historicalChoice` |
| **7.3 死代码警告** | 菜单屏新增 `#data-warn` 横幅，启动后读取并展示 `window.__cohDataWarn`（此前仅设置从未展示）。 | 菜单 HTML + 启动 IIFE |
| **P2-3.1 科技阻力情境化** | `TECH_RESIST_EVENT` 扩展为 `TECH_RESIST_EVENTS` 三套文案（通用 / 宋明理学 / 清中西之辨），按朝代稳定选取（`pickTechResistEvent`）。 | `TECH_RESIST_EVENTS` / `pickTechResistEvent` / `enterMonth` |

**尚未实施（需专项设计 + 数据工程，已在本报告标注）**：P0-1 史材参数化抽取、P0-2/2.2 大事件状态门控与旗标分支、P0-4 `flags/relations` 连锁因果与多结局、P1-5.2 中段加压与多结局评级、P1-7.2 默认凭据去共享化。这些涉及机制重构与 1613 则事件的数据工程，建议在独立迭代中推进。

**待陛下验证**：本地双击 `index.html`（离线/速通模式）与 HTTPS 部署各跑一轮，确认合并调用后事件+后果质量、速通秒开、剧透隐藏与终局对照、数据警告横幅的实际表现。
