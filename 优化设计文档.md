# 《抉择千秋》游玩体验优化设计文档

> 版本：v1.0（基于 2026-08-05 代码基线 `index.html` 14:01）
> 范围：纯玩法 / 系统 / 工程层，不涉及任何画面表现。
> 目标读者：负责实现上述模块的开发者（即陛下与狗噗）。

---

## 0. 文档定位与现状基线

本设计文档承接《游戏体验审查报告.md》的路线图，针对其中 6 个体验命门给出**可直接落地的实现规格**。所有数据结构与函数名均对齐当前 `index.html` 实际代码，确保改起来无需二次推演。

**现状关键事实（来自代码审计 + 全量数据量化）：**

| 项 | 当前值 | 来源 |
|---|---|---|
| 皇帝总数 / 事件总数 | 262 / 1613 | `data/emperors/*.json` |
| 估算总在位月数 | ≈44004 | `Σ(reignEnd-reignStart+1)*12` |
| 硬触史率（预设事件月 / 总月） | **3.67%** | `enterMonth` 仅按 `ev.year===state.year` 命中 |
| 真实感渗透（AI 月引用史实） | ≈0% | AI 日常事件完全自由生成，无史实约束 |
| 终局判定 | 单一 `govAvg` 五维定档 | `endGame` 仅 4 档评级 |
| 决策权重 | 仅改六维数值，不改叙事走向 | `applyOutcome` 只动 `state.stats` |
| 默认凭据 | 单一内嵌密文，全员共享 | `BUILTIN_CRED_CIPHER` + `COH_CIPHER` |

**六个模块与审查报告问题编号的映射：**

| 模块 | 对应问题 | 解决什么体验 |
|---|---|---|
| 1.1 史材参数化抽取 | 1.1 真实感占比过低 | 让 96% 的 AI 月也有史实骨架 |
| 1.2 + 2.2 大事件门控与旗标分支 | 1.2 / 2.2 决策无法改写历史 | 让"选择"真正分叉叙事 |
| 2.1 + 2.4 flags/relations 连锁因果与多结局 | 2.1 / 2.4 无连锁、单结局 | 让抉择首尾呼应、结局多样 |
| 5.2 中段剧情加压 | 5.2 中段无张力 | 让难度曲线有波峰而非一条平线 |
| 7.2 默认凭据去共享化 | 7.2 单点故障 / 隐私 | 去除全员共享密钥风险 |

---

## 1.1 史材参数化抽取

### 设计目标
- **硬触史率**：从 3.67% 提升至 **≥15%**（通过将更多史料锚点固化为可触发的预设事件实现）。
- **史材渗透率**：AI 自由生成的日常/危机事件中，引用真实史材锚点的比例从 0% 提升至 **≥60%**（让"非预设月"也闻得到史实味）。
- 不改变现有 `events` 结构，新增并列字段，向后兼容。

### 核心机制
将每位皇帝的 `background`（传记）与既有 `events`（史实事件）**抽取为结构化「史材锚点」`historicalAnchors`**，作为两件事的燃料：
1. **锚点固化**：高权重锚点直接转为 `events`（带 `year`/`month`/`choices`），提升硬触史率。
2. **锚点约束**：日常/危机 AI 生成时，注入"当前年窗内未消耗的就近锚点"作为生成约束（须出现真实人物、制度、器物），使 AI 月也锚定史实。

### 数据结构与参数定义

**新增于每位皇帝对象（`emperor`）的字段：**

```jsonc
{
  // …既有字段…
  "historicalAnchors": [
    {
      "id": "xia_yu_fangfeng",        // 全局唯一
      "year": -2070,                   // 建议触发的基准年（可浮动 ±2）
      "month": null,                   // 可选，null 表示全年任意月
      "topic": "涂山会盟·立威",         // 锚点主题（用于提示）
      "figures": ["防风氏", "伯益"],     // 须出现的真实人物
      "keywords": ["九鼎", "五服", "朝觐"], // 须涉及的制度/器物/概念
      "premise": "禹大会诸侯于涂山，防风氏后至，禹欲正号令以立威。",
      "weight": 3,                      // 1=背景点缀 2=重要 3=可固化为事件
      "consumed": false                // 运行时标记，不入源文件
    }
  ],
  "materialTarget": 8                  // 该帝期望的硬触史事件数下限（默认 max(6, ceil(在位年/3))）
}
```

**真实感量化指标（落地后须在结算页展示）：**
- `hardHistoryRate = 触发过的预设/锚点事件月 / 总游玩月`
- `anchorCoverage = AI 月中成功注入锚点约束的月 / AI 月总数`

### 处理逻辑

1. **抽取管线（构建期，`tools/extract_anchors.py`）**
   - 读取每个 `emperor.background` 与 `emperor.events`。
   - 用 LLM 批量将传记段落切分为锚点（prompt 要求输出 `historicalAnchors` JSON 数组，约束：人物/制度不可穿越、须带 `year` 估算）。
   - 对 `weight>=3` 的锚点，进一步生成 `choices[5]`（复用现有史实事件写法），直接并入 `events`。
   - 人工轻量校验后写回 JSON。

2. **运行时注入（`enterMonth` + `PROMPTS.user`）**
   - `enterMonth` 选定事件类型后，若非数值危机，调用 `pickAnchor()`：
     ```js
     function pickAnchor(){
       const yr = state.year;
       const cand = state.emperor.historicalAnchors
         .filter(a => !a.consumed && Math.abs((a.year||state.year)-yr) <= 2);
       if(!cand.length) return null;
       // 优先 weight 高且最接近当前年的
       cand.sort((x,y)=> (y.weight - x.weight) || (Math.abs(x.year-yr)-Math.abs(y.year-yr)));
       const a = cand[0];
       a.consumed = true;            // 运行时标记，避免重复
       return a;
     }
     ```
   - 选定锚点存入 `state._activeAnchor`，`PROMPTS.user` 在生成日常/危机事件时追加约束段：
     ```
     本月须围绕史材展开：<premise>。
     须出现的真实人物：<figures>；可涉及的制度/器物：<keywords>。
     不可引入该时代之后的人事物。
     ```

### 落地步骤
1. 新建 `tools/extract_anchors.py`，跑通单帝抽取样例（建议先拿 `01-xia-shang-zhou-qin.json` 试）。
2. 给 `emperor` 数据 schema 增加 `historicalAnchors` / `materialTarget` 字段；用脚本批量为 262 帝生成锚点（先自动后人工抽检 10%）。
3. 改造 `enterMonth`：在"日常政务"分支前调用 `pickAnchor()`，结果挂到 `state._activeAnchor`。
4. 改造 `PROMPTS.user`：日常/危机分支读取 `state._activeAnchor` 注入约束段（历史人物约束现已在 `system` 层，本处补"当月具体锚点"）。
5. 结算页（`endGame`）新增 `hardHistoryRate` 与 `anchorCoverage` 两行展示。

### 预期效果
- 硬触史率 ≥15%，史材渗透率 ≥60%；玩家即便在"日常月"也会遇到真实人物与制度，历史真实感从"背景板"变为"空气"。
- 锚点 `consumed` 机制保证同一史实不重复刷屏；`±2 年`浮动避免与固定 `events` 抢年。

---

## 1.2 + 2.2 大事件状态门控与旗标分支

### 设计目标
- 让"重大决策"真正分叉历史：同一场转折，选 A 走向统一、选 B 走向偏安。
- 让事件出现具备**条件性**（门控），而非纯按年份平铺；历史走向随旗标改写。

### 核心机制
- 事件分级 `tier`：`日常` / `局势` / `major`（转折点）。仅 `major` 参与门控与分支。
- **门控 `gating`**：事件出现的前置条件（旗标/数值/关系）。
- **分支 `branch`**：每个选项可置位/清除旗标，并可指定 `nextEventId` 直接串联下一事件（跳过常规月推进），形成叙事链。

### 数据结构与参数定义

```jsonc
{
  "year": -1046,
  "title": "牧野之决",
  "tier": "major",
  "gating": {
    "requiresFlags": [],                 // 须全部置位
    "forbidFlags": ["zhou_wen_wang_peace"], // 任一置位则不出
    "minStats": { "military": 40 },       // 数值下限
    "minRelation": { "商遗贵胄": -20 }     // 关系下限（见 2.1）
  },
  "choices": [
    {
      "text": "亲率锐师渡孟津，誓师伐纣",
      "branch": { "setFlags": ["zhou_conquest_war"], "clearFlags": [], "nextEventId": "zhou_pacify_shang" }
    },
    {
      "text": "胁商而盟，挟天子以令诸侯",
      "branch": { "setFlags": ["zhou_conquest_treaty"], "nextEventId": null }
    }
  ]
}
```

`state` 新增字段（`newState` 中初始化）：
```js
flags: {},        // { flagId: true }
relations: {},    // { factionId: -100..100 }  // 见 2.1
_branchQueue: null // 待串联的 nextEventId
```

### 处理逻辑

1. **`newState` 初始化** `flags:{}`、`relations:{}`（relations 可由 `emperor.initRelations` 预填）。
2. **`enterMonth` 门控优先**：在现有 `危机 → 年份命中 → 科技阻力 → 日常` 顺序前，插入"门控大事件"判定：
   ```js
   function pickGatedMajor(){
     return state.emperor.events.find(ev =>
       ev.tier === "major" &&
       !state.triggeredEvents.includes(ev.id || ev.title) &&
       gateOK(ev.gating)
     );
   }
   function gateOK(g){
     if(!g) return true;
     if(g.requiresFlags && !g.requiresFlags.every(f=>state.flags[f])) return false;
     if(g.forbidFlags && g.forbidFlags.some(f=>state.flags[f])) return false;
     if(g.minStats) for(const k in g.minStats) if(state.stats[k] < g.minStats[k]) return false;
     if(g.minRelation) for(const f in g.minRelation) if((state.relations[f]||0) < g.minRelation[f]) return false;
     return true;
   }
   ```
3. **分支执行（`makeChoice` 中）**：命中 `outcomes[i]` 结算后，应用分支：
   ```js
   const br = currentEvent.choices[i].branch;
   if(br){
     (br.setFlags||[]).forEach(f=>state.flags[f]=true);
     (br.clearFlags||[]).forEach(f=>delete state.flags[f]);
     if(br.nextEventId) state._branchQueue = br.nextEventId;
   }
   ```
4. **串联推进**：`nextMonth` 开头检查 `state._branchQueue`：若有，直接 `loadEvent` 该串联事件（仍走门控/AI 流程），清空队列，**不推进月份**（叙事链发生在"同一决策的余波"内）。

### 落地步骤
1. 数据侧：为现有 1613 事件中约 8–12% 标 `tier:"major"`，并补 `gating` 与 `branch`（先用 `01` 文件试点，定模板后批量）。
2. 代码侧：`newState` 加 `flags/relations/_branchQueue`；`enterMonth` 插入 `pickGatedMajor` 优先于年份命中；`makeChoice` 结算后调用 `applyBranch`；`nextMonth` 处理 `_branchQueue`。
3. 兼容：无 `tier`/`gating`/`branch` 的既有事件按原逻辑（年份命中）运行，零回归。

### 预期效果
- 同一皇帝可走出不同历史线；"选择"从数值微调升级为叙事分叉。
- 门控避免"未打仗却触发战争大事件"的破绽，历史自洽性提升。

---

## 2.1 + 2.4 flags / relations 连锁因果机制与多结局设计

### 设计目标
- **连锁因果**：早期抉择通过 `flags` 与 `relations` 在数十月后回响（如早年打压士族 → 中年变法受阻）。
- **多结局**：同一皇帝因分支选择不同，终局可为"一统之主 / 守成中主 / 变法身死 / 亡国殇君"等，而非永远按五维均值定档。

### 核心机制
- `state.flags`：离散叙事状态（布尔），由 `branch.setFlags` 写入、门控读取。
- `state.relations`：阵营/人物好感度数值（−100~100），由选项 `relationDelta` 与事件后果共同推动；影响后续事件门控与 AI 生成语气。
- **连锁**：某些 `major` 事件的 `gating` 引用 `relations` 阈值；AI 生成日常事件时注入当前 `relations`，使朝局张力自然延续。
- **多结局**：`endGame` 改用 `ENDING_RULES` 表，按 `flags + relations + stats` 综合判定，原五维均值定档降为兜底。

### 数据结构与参数定义

**皇帝预置关系（`emperor.initRelations`）：**
```jsonc
"initRelations": { "宗室": 50, "士大夫": 40, "边将": 30, "商贾": 10 }
```

**选项关系增量（`choice.relationDelta`）：**
```jsonc
{ "text": "废丞相，权归六部", "relationDelta": { "士大夫": -15, "宗室": -5 } }
```

**结局规则表（代码中新增 `ENDING_RULES`）：**
```js
const ENDING_RULES = [
  { id:"unify",   title:"一统寰宇",
    need:{ flags:["conquest_done"], minStats:{military:55} },
    review:"帝奋武扬威，混一宇内，开创之君也。" },
  { id:"reform_dead", title:"变法身死",
    need:{ flags:["reformed"], maxRelation:{ "士大夫":-40 } },
    review:"帝锐志更化，然触怒清议，谤议沸腾，功败垂成。" },
  { id:"long_peace", title:"治世仁主",
    need:{ flags:["pacified"], minStats:{people:60,court:60} },
    review:"帝躬行仁恕，海内乂安，号为治世。" },
  { id:"usurped", title:"权臣篡立",
    need:{ maxRelation:{ "边将":-30 }, minStats:{military:30} },
    review:"威不逮下，爪牙生心，社稷终移他姓。" }
  // …可扩展 8–12 条
];
function evalEnding(st){
  for(const r of ENDING_RULES){
    if(r.need.flags && !r.need.flags.every(f=>st.flags[f])) continue;
    if(r.need.minStats) for(const k in r.need.minStats) if(st.stats[k]<r.need.minStats[k]) continue/*但需整体满足*/;
    // 简化：逐个条件判定
    let ok=true;
    if(r.need.flags && !r.need.flags.every(f=>st.flags[f])) ok=false;
    if(r.need.minStats) for(const k in r.need.minStats) if(st.stats[k]<r.need.minStats[k]) ok=false;
    if(r.need.maxRelation) for(const f in r.need.maxRelation) if((st.relations[f]||0) > r.need.maxRelation[f]) ok=false;
    if(r.need.minRelation) for(const f in r.need.minRelation) if((st.relations[f]||0) < r.need.minRelation[f]) ok=false;
    if(ok) return r;
  }
  return null; // 交回原五维定档
}
```

### 处理逻辑
1. **关系推动**：`makeChoice` 结算分支后，应用 `choice.relationDelta`：
   ```js
   const rd = currentEvent.choices[i].relationDelta;
   if(rd) for(const f in rd) state.relations[f] = clamp((state.relations[f]||0)+rd[f], -100, 100);
   ```
2. **连锁门控**：`gateOK` 已支持 `minRelation`/`maxRelation`（见 1.2）。
3. **AI 语气延续**：`PROMPTS.user` 日常/危机分支追加：
   ```
   当前朝局关系：<relations 摘要，如 士大夫:-20(离心) 边将:45(效忠)>。
   事件须反映上述关系的张力。
   ```
4. **多结局判定**：`endGame` 开头：
   ```js
   const end = evalEnding(state) || defaultVerdictByGovAvg(state);
   document.getElementById("end-verdict").textContent = end.title;
   document.getElementById("end-review").textContent = end.review;
   ```
   原 `govAvg` 四档逻辑保留为 `defaultVerdictByGovAvg` 兜底。

### 落地步骤
1. 数据：为 `major` 事件选项补 `relationDelta`；为皇帝补 `initRelations`（可先用默认模板 `{宗室:50,士大夫:40,边将:30}` 批量注入，再按史实微调）。
2. 代码：`newState` 加 `relations`；`makeChoice` 应用 `relationDelta`；`gateOK` 支持关系阈值；`PROMPTS.user` 注入关系摘要；新增 `ENDING_RULES` + `evalEnding`，`endGame` 改调。
3. 调参：先放 4–6 条结局规则覆盖主路径，跑通"同帝不同结局"自测。

### 预期效果
- 抉择产生"蝴蝶效应"，叙事厚度显著上升。
- 终局从 4 档扩到 8–12 种，重玩价值与历史沉浸感增强；原五维定档仍作为兜底，不丢公平性。

---

## 5.2 中段剧情加压策略

### 设计目标
- 当前难度曲线前松后紧且中段平坦（仅晚年健康衰减）。引入**中段压强波峰**，让玩家在在位中期（约 30%–75% 处）持续面对危机与抉择疲劳。
- 压制"无脑堆高数值"的退化策略，制造真实统治张力。

### 核心机制
- 引入 `pressure`（0~1）标量，由在位进度与当前数值健康度合成。
- 中段压强提升：危机事件触发概率、负面后果幅度、`relations` 驱动的连锁恶化。
- 预埋"中段定时炸弹"：在约 50% 在位年放置一个 `major` 危机锚点（如继承危机、天灾、边叛），无视数值强制出现。

### 数据结构与参数定义

```js
// 运行时（newState 中）
pressure: 0

// 皇帝可选预埋（emperor.midCrisisAnchors）
"midCrisisAnchors": [
  { "atProgress": 0.5, "event": { "title":"巫蛊之祸", "tier":"major", "gating":{}, "choices":[/*…*/] } }
]
```

`pressure` 合成公式：
```js
function computePressure(st){
  const span = st.emperor.reignEnd - st.emperor.reignStart;
  const prog = (st.year - st.emperor.reignStart) / Math.max(1, span);
  // 中段(0.3~0.75)抬升为高斯峰
  const bell = Math.exp(-Math.pow((prog-0.5)/0.18, 2));
  const healthFactor = 1 - avgStat(st.stats)/100; // 数值越差越加压
  return clamp(0.35*bell + 0.4*healthFactor, 0, 1);
}
```

### 处理逻辑
1. **`enterMonth` 压强注入**（在现有类型判定中插入）：
   ```js
   state.pressure = computePressure(state);
   // 非危机月，中段按压强提升危机概率
   if(!crisis && eventType==="日常政务" && state.pressure>0.4 && Math.random() < 0.35*state.pressure){
     eventType = "数值危机";
   }
   ```
2. **后果幅度放大**：`applyOutcome` 中危机事件的 `limit` 由固定 25 改为随压强上浮：
   ```js
   const limit = currentEvent.source==="数值危机" ? Math.round(25 + 8*state.pressure) : 15;
   ```
3. **定时炸弹**：`enterMonth` 在门控大事件判定后、年份命中前，检查 `midCrisisAnchors` 是否已到 `atProgress` 且未触发，到则强制以该事件登场。
4. **关系连锁恶化**：压强高时，若 `relations` 中某阵营 <0，日常事件更易触发该阵营相关的 `major` 门控（已在 2.1 `gateOK` 支持）。

### 落地步骤
1. `newState` 加 `pressure`；新增 `computePressure`。
2. `enterMonth` 改造：先计算 `pressure` → 中段危机概率注入 → `midCrisisAnchors` 定时炸弹判定（置于门控大事件之后、年份命中之前）。
3. `applyOutcome` 危机 `limit` 改为压强相关；`loadEvent` 的危机提示文案随压强变化（如"四方烽烟并起"）。
4. 数据：为 5–10 位代表性皇帝补 `midCrisisAnchors` 试点，验证波峰手感。

### 预期效果
- 难度曲线呈"中段隆起"而非平线，统治张力明显；玩家不能靠前期堆数值躺赢。
- 定时炸弹保证每位皇帝都有标志性的中年危机，叙事记忆点增强。

---

## 7.2 默认凭据去共享化方案

### 设计目标
- 当前 `BUILTIN_CRED_CIPHER`（XOR+base64，密钥 `COH_CIPHER.K` 内嵌源码）**全员共享同一密钥**，存在单点故障：一旦泄露/被限流，所有用户请求受阻；且无法按用户配额/撤销。
- 目标：**仓库零内嵌凭据** + **每用户独立额度/可撤销** + **数据流透明可告知**。

### 核心机制（推荐方案：部署期注入 + 每用户配置下发）
采用双轨：
- **A 轨（开源/自托管）**：仓库不提交任何凭据；自托管者通过部署环境变量注入自有 key，`settings.builtin` 默认关闭。
- **B 轨（官方托管版）**：首次启动向轻量 `provisioning` 服务申请**按设备指纹绑定的短期 token**（带配额与限流），共享主密钥仅存服务端。客户端只持本机 token。

> 不推荐"每端加盐混淆"的伪去共享——底层仍是同一把共享密钥，只能增加破译成本，不能消除单点故障。

### 数据结构与参数定义

**构建期配置（不入 git，`.gitignore` 忽略）：**
```jsonc
// coh_config.json（部署时生成）
{
  "provisionUrl": "https://provision.coh.example.com/token",
  "apiBase": "https://api.coh.example.com",
  "defaultModel": "agnes-2.5-flash",
  "selfHosted": false
}
```

**运行时凭据状态（`state.provision`，存 localStorage）：**
```js
state.provision = {
  token: "eyJ...",      // 每用户短期 token
  expiresAt: 1712000000000,
  quota: 500            // 剩余额度
};
```

**`settings` 变更：**
```js
// 移除 settings.builtin 默认值 true 的"静默共享"语义
// 新增 settings.selfHosted（自托管者填自己 key）
```

### 处理逻辑

1. **移除源码内嵌**：删去 `BUILTIN_CRED_CIPHER` 与 `COH_CIPHER` 中硬编码密钥 `K`；改为从 `coh_config.json`（构建注入）或环境变量读取。对外开源版本 `coh_config.json` 仅含占位。
2. **配置下发（`resolveCreds` 重写）：**
   ```js
   async function resolveCreds(){
     if(settings.selfHosted){
       return { api:settings.api, key:settings.key, model:settings.model||CFG.defaultModel, temp:settings.temp };
     }
     // B 轨：每用户 token
     if(!state.provision || state.provision.expiresAt < Date.now()){
       state.provision = await fetchProvisionToken(); // 设备指纹换取
     }
     return { api:CFG.apiBase, key:state.provision.token, model:CFG.defaultModel, temp:settings.temp };
   }
   async function fetchProvisionToken(){
     const fp = await deviceFingerprint(); //  stable per-browser
     const r = await fetch(CFG.provisionUrl, {method:"POST", body:JSON.stringify({fp})});
     if(r.status===429) throw new Error("额度已用尽，请明日再试或填入自有 key");
     return r.json(); // {token, expiresAt, quota}
   }
   ```
3. **`hasCreds` 重写**：
   ```js
   function hasCreds(){
     if(settings.selfHosted) return !!(settings.api && settings.key);
     return !!(state.provision && state.provision.token && state.provision.expiresAt>Date.now());
   }
   ```
4. **UI 透明化**：设置页新增"激活免费额度"按钮（替代原静默 shared key）；点击后展示"请求将发往 <apiBase>，由官方按设备配额计量"，并明示"亦可填入自有 key，走你自己的账户"。
5. **离线兜底不变**：`offlineMode` 与 `fastMode` 仍可在零凭据下完整体验（走本地预设/后备池），不依赖任何网络凭据。
6. **限流友好**：`callAI` 捕获 429 → 友好提示 + 建议切离线/自填 key，**不再回退到共享密钥**。

### 落地步骤
1. 抽离 `COH_CIPHER.K` 与 `BUILTIN_CRED_CIPHER` 至 `coh_config.json`（gitignore），提供 `coh_config.sample.json` 占位。
2. 新增 `provisioning` 服务端（最小 serverless：设备指纹 → 签发短期 token + 配额表 + 限流）。
3. 重写 `resolveCreds` / `hasCreds` / `callAI` 的 429 分支；设置页 UI 改造。
4. `settings.builtin` 默认语义改为"官方额度"（需激活）或"自托管"，不再静默共享。
5. 全量自测：无网络（离线可用）、有激活 token、自填 key、token 过期刷新、429 限流 五条路径。

### 预期效果
- 仓库无共享密钥，单点故障与隐私风险消除。
- 每用户独立额度、可单独撤销、可限流；数据流对玩家透明。
- 离线/速通体验不受任何影响（本就零凭据依赖）。

---

## 7. 跨模块集成与落地顺序

六个模块并非孤立，依赖关系如下（箭头表示"先于"）：

```
7.2 凭据去共享  ──（独立，可最先或最后，不阻塞玩法）
        │
1.1 史材抽取 ──► 1.2/2.2 门控分支 ──► 2.1/2.4 flags·relations·多结局
        │                                    ▲
        └──────────── 5.2 中段加压 ──────────┘（加压引用 relations 与 major 门控）
```

**建议落地批次：**

| 批次 | 模块 | 理由 |
|---|---|---|
| P1（地基） | 7.2 凭据去共享 | 工程风险独立，越早去单点故障越好 |
| P2（骨架） | 2.1/2.4 flags·relations·多结局 | 其余玩法模块都依赖 `state.flags/relations` 与 `evalEnding` |
| P3（分叉） | 1.2/2.2 门控与旗标分支 | 依赖 P2 的 flags/relations |
| P4（真实感） | 1.1 史材抽取 | 独立，但锚点可作为 `major` 事件素材反哺 P3 |
| P5（张力） | 5.2 中段加压 | 依赖 P2/P3 的 relations 与 major 门控 |

**关键集成点（实现时务必对齐）：**
- `enterMonth` 新顺序：**危机 → 门控 major（gateOK 含 relations）→ 中段定时炸弹 → 年份命中历史 → 科技阻力 → 日常（注入 `_activeAnchor`）**。
- `makeChoice` 结算顺序：`applyOutcome`（数值）→ `applyBranch`（flags + _branchQueue）→ `applyRelationDelta`（relations）。
- `PROMPTS.user` 同时注入 `_activeAnchor`（1.1）与 `relations` 摘要（2.1），AI 月既真实又有朝局张力。
- `endGame` 调用 `evalEnding`（2.4），并展示 `hardHistoryRate`/`anchorCoverage`（1.1 指标）。

---

## 8. 验证指标与验收口径

| 模块 | 验收指标 | 达标线 |
|---|---|---|
| 1.1 | `hardHistoryRate` | ≥15% |
| 1.1 | `anchorCoverage`（AI 月注入锚点占比） | ≥60% |
| 1.2/2.2 | 同帝不同 `flags` 线可达性 | 至少 2 条可触达分支链 |
| 2.1/2.4 | 可触发结局种类数 | ≥6（原 4 档之外新增 ≥4） |
| 2.1/2.4 | 关系连锁回响（早期 choice 影响 ≥10 月后事件门控） | 自测可复现 ≥1 例 |
| 5.2 | 中段危机月占比 vs 前/后段 | 中段显著 > 前后段（U/峰形） |
| 7.2 | 仓库内嵌共享密钥 | 0（grep `BUILTIN_CRED_CIPHER` 为空） |
| 7.2 | 五条凭据路径自测 | 全绿 |

**回归保障**：所有新增字段（`tier`/`gating`/`branch`/`relationDelta`/`historicalAnchors` 等）均为可选，缺失时走原逻辑；合并 AI 调用、速通跳过、剧透隐藏、警告横幅等既有优化不受影响。

---

> 文档结束。各模块函数签名与字段名已对齐 `index.html` 当前实现，开发者可直接据此开分支落地。建议按 P1→P5 批次推进，每批合入后跑对应验收指标。
