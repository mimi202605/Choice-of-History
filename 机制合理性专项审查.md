# 《择决千秋》系统与机制合理性专项审查（2026-08-06）

> 审查视角：系统 / 数值 / 机制设计（game designer 视角），不谈画面。
> 方法：通读 `index.html` 核心系统（`enterMonth` / `makeChoice` / `applyOutcome` / `applyCoupling` / `nextMonth` / `endGame` / `detectCrisis` / `computePressure` / `evalEnding` / `gateOK`），并对**运行时实际加载的数据** `data/emperors/coh_data.js`（262 帝 / 1866 事件）做字段级量化。所有结论附代码行号或数据证据。
> 定位：本审查聚焦"机制是否合理"，与既有《游戏体验审查报告》《优化设计文档》互补——后者已覆盖 UX 层，本篇补**系统/数值层的硬伤**。

---

## 〇、一句话结论

骨架已搭好，但三处"非常不合理"让骨架失去约束力：

1. **只有健康能致死，其余 5 维归零零代价** → 平衡张力塌掉，"堆数值躺赢"是结构性必然。
2. **多结局被数值阈值架空，旗标链是幽灵** → 480 个 `setFlags` 全是自动生成的局部标记，6 个结局旗标 **0 次**被置位，结局 100% 由终局数值决定。
3. **数值 clamp 与 AI 契约 / 设计文档三套数字互相打架，且全是 magic number** → 同一常量存在 ±15、±25、±10、±(12+5p) 四种说法，无一有 rationale。

另有两处重大但次一级：健康自然衰减对 **96%** 皇帝不触发（`ascendAge` 全缺）；"中段加压"随机危机注入**根本没落地**，压力只让数值上限 12→17，等于空头支票。

---

## 一、严重程度速览

| # | 问题 | 严重度 | 核心证据 |
|---|------|--------|----------|
| P0-1 | 仅健康致死，5/6 维度零代价 | 非常不合理 | `endGame` 仅 `"驾崩"`/`"弃位"`；`detectCrisis`（L2119）只换事件类型、不结束；危机事件自带自愈 |
| P0-2 | 多结局被数值架空，旗标链是幽灵 | 非常不合理 | 6 个结局旗标 `setFlags` 出现 **0 次**；480 个 `setFlags` 全是 `did_*` 局部标记；`evalEnding` 靠 `derived`（纯数值）兜底 |
| P1-1 | clamp 三套数字打架 + 全是 magic number | 非常不合理 | prompt ±15/±25（L1878）vs 代码 ±10/±(12+5p)（L2892）vs 设计文档 ±15/±(25+8p)；权重 0.35/0.4/5 无 rationale |
| P1-2 | 健康自然衰减对 96% 皇帝不触发 | 不合理 | `ascendAge` 0/262（默认 25）；仅 `reign≥45y` 才到 70 岁；252/262 在位 <45y |
| P1-3 | "中段加压"是空头支票 | 不合理 | 设计文档的 `0.35*p` 随机危机注入**代码里不存在**（grep 无命中）；`pressure` 仅让危机上限 12→17 |
| P2-1 | 数值耦合太浅 | 偏薄 | `applyCoupling`（L2935）仅 3 条、阈值极端（>75 / <25 / <20） |
| P2-2 | 真实感仍只改 flavor 不改结构 | 偏薄 | 锚点仅注入 AI 约束文本，不增加 `historyEventMonths`；分支链 93% 门控为空 |

---

## 二、P0-1：只有健康能致死，平衡张力塌方

### 现象
- `endGame` 的所有调用点：仅 `endGame("驾崩")`（L3010、L3053）与 `endGame("弃位")`（L3066）。**没有任何路径因 国库/民心/军事/朝政 归零而结束游戏。**
- `detectCrisis`（L2119）在数值 ≤10 时只返回一串文案，作用仅是让 `enterMonth` 把事件类型切成"数值危机"——它**不产生任何失败后果**。
- 更糟：危机事件自带"自愈"。`fallbackOutcome`（L2949）对危机事件给最低维 `+6`；`applyOutcome` 的"安全网"（L2886）保证每个选项至少 `+2` 到某个弱项。于是**国库=0 的皇帝，下个月自动触发危机事件，危机事件又把他奶回来**——归零不仅不致死，反而成为无限续命的触发器。

### 为什么不合理（game-design rationale）
这是 Reigns 类"六维平衡"品类的命门：**乐趣来自"每根线都是生命线，任何一根断就完"的达摩克利斯之剑**。本作把 6 根线砍成 1 根（仅健康）。其余 5 维无论怎么烂，都不会让你"输"，只会让你反复看到危机事件然后被奶满。结果：
- 玩家可以放任 国库/民心/军事/朝政 长期贴地，照样通关拿"明君"（只要健康撑住、avg≥55 触发健康回血）。
- "输的逼近感"消失，报告 5.2 / 8.4 担心的"无脑堆高数值躺赢"成为**结构性必然**而非个别现象。

### 优化思路
给 5 个治理维度装上"可升级的失败链"，而非瞬间致死（也更贴合历史：皇帝不会因国库空而秒死，但会因此亡国）：

| 阶段 | 触发 | 后果 | rationale |
|------|------|------|-----------|
| 预警 | 某治理维 ≤10 | 进入"危局"事件池（已有） | 给玩家修正窗口 |
| 恶化 | 同一维连续 ≤10 达 **K 月** | 升级为"民变/朝变/边叛"major 事件，后果幅度 ×**1.5** | 复用现有 `lowStreak` 机制；K=[PLACEHOLDER·建议 3–4 月，验证路径：playtest 看能否在 2 个好选择内救回] |
| 崩盘 | 同一维 ≤5 再持续 **K 月** | `endGame("亡国")` 或 `endGame("逼宫")` | 让 5 维真正"致命"，但留足反应时间 |

要点：**失败链必须写在 `nextMonth` 里（像 `lowStreak` 那样按月累计），不能只靠危机事件的自愈逻辑**。同时把"安全网 +2"改为"只在非危机事件生效"，避免危机被奶穿。

---

## 三、P0-2：多结局被数值架空，旗标链是幽灵

### 现象（数据铁证）
对运行时 1866 事件全量扫描：
- 480 个 `branches` 里的 `setFlags` 值，**100% 是自动生成的事件局部标记**，形如 `did_xia_yu_e2_0`、`did_xia_qi_e3_4`——每个事件每个选项一个，互不通用。
- `evalEnding`（L3072-3109）需要的 6 个语义旗标 `unify / reform / long_peace / usurped / scholar_oppose / weak`，**在整个 262 帝数据里被 `setFlags` 置位的次数为 0**。
- `evalEnding` 因此只能靠 `derived`（L3096-3103）从**终局数值**反推旗标（如 `unify = military≥70 && treasury≥55`）。即：结局 = 终局数值阈值，和"你做过什么选择"无关。

### 为什么不合理
优化设计文档 2.1/2.4 的核心承诺是"抉择首尾呼应、结局由分支选择驱动"。实际落地：
- 旗标系统在代码里完整（flags/relations/branchQueue 都在），数据里也大量置位（480 处）——**但置的都是无人读取的局部标记**，`gateOK`（L2455）判的门控 93% 是空 `{}`，6 个结局旗标更从未被写入。
- 结果：你早年"诛杀功臣"的旗标不会在数十年后回响；你走"变法"线还是"守成"线，只要终局数值相近，结局文本完全一样。**"选择有重量"在终局层是假的。**

### 优化思路
二选一，不要骑墙：

**方案 A（推荐，保留叙事承诺）：建一份"规范旗标词表"，让数据说人话。**
只定义 8–12 个跨事件通用的语义旗标，每条由具体事件置位、被后续事件门控、被结局读取：

| 规范旗标 | 由何置位（举例） | 门控何事件 | 通向结局 |
|----------|------------------|------------|----------|
| `conquest` | 亲征灭国类选项 | 后续"边疆巩固/叛乱"事件 | 一统寰宇 |
| `reformed` | 变法/新政类选项 | 后续"清议反弹"事件 | 鼎革之主 |
| `scholar_resent` | 触怒士大夫类选项 | 后续"台谏攻讦"事件 | 清议倾国 |
| `general_loyal` / `general_resent` | 恩遇/猜忌边将 | 后续"兵变"事件 | 权臣篡立 |
| `heir_crisis` | 继承安排失当 | 后续"夺嫡"事件 | 季世之主 |
| `people_content` | 长期惠民 | 后续"治世祥瑞"事件 | 治世仁主 |

写法：给每个 `major` 事件补 `branches[i].setFlags`（用上表词），给后续事件补 `gating.requiresFlags/forbidFlags`，给 `ENDING_RULES` 的 `need.flags` 用上表词。这是**数据工程**，但只占 516 个 major 事件的一小部分需要真正联网。
验证：跑"同帝不同线"自测，确认同一皇帝能因选择不同抵达 ≥2 个结局。

**方案 B（诚实降级）：承认结局是数值驱动，删掉旗标伪装修饰。**
若不想做方案 A 的数据工程，就把 `evalEnding` 的 `st.flags` 合并逻辑去掉，纯用 `derived` 数值判定，并在文档里明说"结局由你的治国成绩决定"。**至少不制造'选择能改变结局'的虚假期待。** 当前半吊子状态最差：代码看似支持选择驱动，实则永远走数值兜底。

---

## 四、P1-1：数值 clamp 三套数字打架，全是 magic number

### 现象
同一"单次事件数值变动上限"常量，存在四种说法：

| 来源 | 常规事件上限 | 危机事件上限 |
|------|--------------|--------------|
| AI 生成契约（`PROMPTS.user`，L1878） | **±15** | **±25** |
| 实际代码（`applyOutcome`，L2892） | **±10** | **±(12 + 5·p)**（即 12~17） |
| 优化设计文档 5.2（L341） | **±15** | **±(25 + 8·p)** |
| 旧体验报告 5.2 | "clamp(±15)" | — |

代码实际采用最保守的一套（±10 / 12~17），且 `10 / 12 / 5 / 0.35 / 0.4`（后两者是 `computePressure` 权重，L2440）**全部无 rationale**。

### 为什么不合理
1. **契约违约**：AI 被要求生成 ±15/±25，代码却截断到 ±10/±17。AI 写"国库大损"时实际只掉 10——**叙事文本与生效数值错位**，玩家读到夸大的后果、看到缩水的数字，信任感被缓慢侵蚀。
2. **无法调参**：因为三个来源数字都不一样、又都没解释，任何后续 tuning 都失去基准——你改的是哪一套？
3. **magic number 违反设计纪律**：`10`、`12`、`5`、`0.35`、`0.4` 凭空出现，没有"为什么是它"的解释。

### 优化思路
- **统一契约**：AI prompt 的幅度必须 **等于** 代码 clamp，禁止出现"让 AI 生成 ±25 但只应用 ±17"。先定一个 clamp，再把 prompt 改成同一数字。
- **给 clamp 一个 rationale（非 magic number）**：建议基准——
  - 常规事件 **±10**：100 分满条上单次移动 10%，约 5 个月可从 50 摆到 100，足够"有感"又不难恢复。
  - 危机事件 **±20**（[PLACEHOLDER·待 playtest]）：危机本就该是真正的冲击，±10 和常规没区别就失去"危机"语义。
  - 压力放大改为危机上限 `20 + 8·p`（p∈[0,1] → 20~28），让中段危机确实更痛。
  - 所有数字标 `[PLACEHOLDER · 验证路径：用 Monte Carlo 模拟 500 局，看 avg 归零/触顶分布是否落在目标区间]`。
- `computePressure` 的 `0.35/0.4` 同样需要 rationale：它们决定"中段隆起"与"数值越差越加压"的权重比，当前只是拍脑袋。建议改为可解释的：中段峰贡献与数值健康度贡献应为**可加且封顶 1**，权重比按"你希望中段压迫占比多少"反推，而非随手 0.35/0.4。

---

## 五、P1-2：健康自然衰减对 96% 皇帝不触发

### 现象
- `nextMonth`（L3037）：`ascendAge = emperor.ascendAge!=null ? emperor.ascendAge : 25`。运行时数据 **`ascendAge` 字段 0/262 存在** → 全部走默认 25。
- 自然衰减门槛：年龄 >70 才 `-1`，>80 才再加。年龄 = 25 + 在位年数。即**在位满 45 年才首次衰减**。
- 量化：262 帝中 **252 位（96%）在位 <45 年** → 整个游玩过程健康自然衰减 **永远为 0**。
- 叠加 `avg≥55 → 健康 +1/月`（L3026）：对这 96% 的皇帝，健康是**只涨不跌、稳定爬到 100 的股票**。

### 为什么不合理
- "皇帝龙体"作为六维之一，对绝大多数游玩**没有下行压力**——它消灭了"健康"维度的管理意义（报告 8.3 称其"最被动"，实际是"最自由"）。
- 注释声称"以真实年龄计，消除把在位年数当年龄的语义错误"，但 `ascendAge` 全缺让这个修复**只停留在代码层、数据层从未兑现**，实际仍是 25 + 在位年数的近似。

### 优化思路
- **最低成本修复（纯数据）**：给 262 帝补 `ascendAge`（登基年龄是硬史料，康熙 8 岁、雍正 45 岁……）。补完后，自然衰减门槛（70/80）才对长 reign 真正生效，短 reign 仍偏宽松也符合史实（青年即位者确实不易老死）。
- **机制层补强**：即便补了 `ascendAge`，多数皇帝仍可能因 avg 高而回血。建议加一条"操劳"因果：重大决策（major 事件抉择）按选项 `health` 代价结算（已有 statChanges.health），让"勤政"与"长寿"产生真实 trade-off，而非健康只涨不跌。

---

## 六、P1-3："中段加压"是空头支票

### 现象
- 优化设计文档 5.2 明确要：`enterMonth` 中段按压强**随机提升危机概率**（`if(pressure>0.4 && Math.random() < 0.35*pressure) eventType="数值危机"`）。
- **代码里不存在这段**（grep `0.35*state.pressure` / `pressure>0.4` 全无命中）。
- `pressure` 在代码中的实际作用仅两处：`computePressure` 算值（L2435）+ `applyOutcome` 把危机上限从 12 抬到 17（L2892）。即中段压强**只让危机幅度多 5 点**，难度曲线没有真正的"波峰"。

### 为什么不合理
- 报告 5.2 / 优化文档 5.2 的核心承诺"中段隆起、统治张力波峰"**未交付**。当前中段和前段手感几乎一样，只是危机事件数值略大 5 点——玩家感知不到。
- `midCrisisAnchors`（定时炸弹）是真实有效的（L2476），但它只给个别皇帝埋 1 个中点事件，撑不起"波峰"的全局体感。

### 优化思路
二选一，别半吊子：
- **落实文档方案**：把 `0.35*p` 随机危机注入写进 `enterMonth`（放在 `detectCrisis` 之后、`pickGatedMajor` 之前或之后需定序）。`0.35` 标 [PLACEHOLDER·验证：playtest 中段危机月占比应显著 > 前后段]。
- **或诚实收敛**：若不想做随机注入，就把 `pressure` 改名为"危机烈度"并在文档里说明它只调制危机幅度，去掉"中段加压波峰"的承诺。当前"叫加压实则只改 5 点上限"最误导。

---

## 七、P2：偏薄项（非致命但值得做）

| 项 | 现状 | 优化 |
|----|------|------|
| 数值耦合（L2935） | 仅 3 条、阈值极端（>75/<25/<20） | 加"朝政过低放大其他维负面波动""科技过高持续触发阻力"等 2–3 条，让耦合有"牵一发动全身"体感 |
| 真实感结构（锚点） | 锚点仅注入 AI 约束文本（`pickAnchor` L2443），不增加 `historyEventMonths` | 锚点应可升级为"轻量预设事件"（带 year 窗 + 真实选项），让"亲历历史"在结构上占比提升，而非只改 flavor |
| 史官点评/曲线反馈回循环 | 纯 flavour（报告 3.3） | 点评风格随 `relations` 偏移；曲线异常（某维骤降）触发特殊事件，形成"反馈→玩法"闭环 |

---

## 八、顺手肯定的扎实改进（避免一概否定）

以下已落地且方向正确，审查予以确认：
- 合并 AI 调用（事件+后果单次返回），消除每月第二次网络等待；速通模式秒开（`loadEvent` 早 return）。
- 决策当场隐藏"史实之择"，对照留到终局（`renderPreset` / `endGame`）。
- 菜单屏数据加载警告横幅（`#data-warn` 读 `__cohDataWarn`，修复 7.3 死代码）。
- 终局展示 `触史率 / 史材渗透 / 旗标数 / 关系数`——把"真实感"做成**可量化指标**，是正确的一步（虽底层比例仍低）。
- 科技树增量重算（`recomputeTechProfile`），注入加成不覆盖事件数值，避免数值被悄悄改写。

---

## 九、建议落地批次（按"先修不合理，再补厚度"）

| 批次 | 项 | 理由 | 成本 |
|------|----|------|------|
| **B1（最该先做）** | P0-1 失败链 | 唯一让 5 维"致命"的机制，直接决定游戏是否成立 | 中（写 `nextMonth` 累计逻辑 + 危机幅度×1.5） |
| **B2** | P1-1 clamp 统一 + rationale | 消除契约违约与 magic number，是后续所有 tuning 的地基 | 低（改 1 行 clamp + 1 行 prompt） |
| **B3** | P0-2 规范旗标词表 | 让"选择有重量"在终局兑现；否则多结局永远是数值伪装 | 高（516 major 事件数据工程，可分批） |
| **B4** | P1-2 补 `ascendAge` | 纯数据，低风险高回报 | 低 |
| **B5** | P1-3 落地随机危机注入 或 诚实收敛 | 兑现/收回"中段加压"承诺 | 低 |
| **B6** | P2 耦合加深 / 锚点转轻量事件 | 厚度，非阻塞 | 中 |

**一句话收尾**：这游戏现在"能跑、能玩、不崩"，但平衡张力只剩 1/6、结局选择权是装饰、数值契约自相矛盾——这三件事不修，"抉择有重量、亲历真实历史"的核心卖点就立不住。B1+B2 是性价比最高的两根支柱，建议先打。
