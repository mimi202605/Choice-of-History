# 《择决千秋》技术美术优化规格书 v1.0

> 范围：承接《游戏体验审查报告》《优化设计文档》明确排除的"美术表现"空白地带。
> 技术前提：本作为纯 Web 原生（JS + HTML + CSS + Canvas），**全仓零位图资源**（无 png/jpg/svg/webp），美术 = 100% CSS 渐变 + Canvas 水墨。故"技术美术"落点为：CSS 设计体系/配色、UI 渲染性能（重排/重绘/GPU 合成）、动效 60fps、可视化开销、可访问性、字体资源管线。
> 方法：所有结论均经源码审计（`index.html` / `data/tech_tree_ui.html`），附行号。

---

## 1. 现状审计（已验证）

| # | 问题 | 位置(行) | 严重度 | 影响 | 根因 |
|---|------|---------|--------|------|------|
| 1 | 字体走 CDN | index.html:7–9 | 已缓解 | 游戏联网运行，CDN 字体正常生效；已补系统宋体回退栈(`Songti SC`/`SimSun`)，CDN 偶发慢/失败时优雅降级，不崩美术 | Google Fonts `<link>` + 已补本地回退 |
| 2 | 水墨转场 Canvas 无 DPR 缩放 | index.html:1041–1042 | **P0** | retina/手机(DPR≥2)上墨晕是糊的，直接拉低"美术增强"观感 | `canvas.width=innerWidth` 未乘 `devicePixelRatio` |
| 3 | 粒子常驻无限动画无守卫 | index.html:997 / 49–55 | P1 | 18 个 DOM 层常驻无限动画，低端机 jank + 续航实损；无障碍缺失 | 无 `prefers-reduced-motion`；无 `will-change` |
| 4 | `backdrop-filter` 缺 webkit 前缀 | index.html:128–129 | P1 | iOS/Safari 顶部栏毛玻璃失效 + 滚动每帧重采样背景 | 仅标准属性 |
| 5 | `transition:all` 反模式 | index.html（9 处） | P1 | 任意属性变化都触发过渡，产生意外重绘/动画 | 通配过渡 |
| 6 | 文字对比度未过 WCAG AA | index.html:24 (`--dim-light:#8a7b6a`) | P1 | 小字约 3:1，低于 AA 4.5:1 | 色值过亮 |
| 7 | 科技树视觉与主界面脱节 | tech_tree_ui.html:8–16 | P1 | 深色"开发工具"皮肤 vs 主界面宣纸水墨，品牌不一致 | 独立设计语言 |
| 8 | 科技树输入全量重建 DOM | tech_tree_ui.html:131 / 182 | P2 | 每次击键整网格重建，布局抖动 | `oninput` 直连 `render` 无防抖 |
| 9 | 无 `prefers-color-scheme` 暗色 | 全局 | P2 | 弱光环境刺眼 | 仅浅色主题 |
| 10 | 金/强调色作小号正文对比度不达标 | `--gold`(#9a7b3f) / `--acc`(#87692c) | P1 | 科技树 `.cost`(11px)/`.pill b`/`.bonus`(11px)、主页 `.stat.high` 标签等小字仅 3.2–4.4:1，低于 AA 4.5:1 | 原审计(#6)只修 `--dim-light`，未覆盖金/强调色；由 `tools/contrast_audit.js` 全量扫描发现 |

---

## 2. 本回合已实施修复（零回归）

| 改动 | 位置 | 效果 |
|------|------|------|
| Canvas DPR 缩放（封顶 2x）+ CSS 尺寸分离 | index.html:1041–1045 | 墨晕锐利；填充率受控（3x 屏不爆） |
| 粒子 18→12（桌面）/ 8→6（移动）+ `will-change:transform,opacity` | index.html:997 / 49–55 | 合成层减半，续航改善 |
| `prefers-reduced-motion` 守卫（关粒子无限动画/屏切换） | index.html:72–75 | 无障碍 + 省电 |
| `backdrop-filter` 加 `-webkit-` 前缀 | index.html:128–129 | iOS/Safari 毛玻璃生效 |
| `transition:all` → 显式属性列表（全 9 处） | grep 残留 = 0 | 消除意外过渡/重绘 |
| `--dim-light` `#8a7b6a` → `#6f6151` | index.html:24 | 对比度 ≈5.3:1，过 AA |
| 字体回退栈强化：`'Ma Shan Zheng'`/`'Noto Serif SC'` → 追加 `'Songti SC','SimSun'` | 全局（grep `cursive`=0） | 离线标题/正文落到系统宋体，而非随机 `cursive`（**临时缓解**，见 §3-P0） |
| 科技树 `pts` 输入防抖 120ms | tech_tree_ui.html:131 | 消除击键布局抖动 |
| `--gold` `#9a7b3f`→`#7d5e28`；`--acc` `#87692c`→`#7a5a24` | index.html/tech_tree_ui.html `:root` | 金/强调作小号正文达标（bg 上 5.1/5.4:1）；全站 9 处对比度不达标归零，且沉金更贴水墨宣纸调性 |

---

## 3. 决策记录（按陛下指示更新）

### 字体 CDN（原 P0，已取消）
- **陛下明确：游戏联网玩，不考虑断网。** 故字体 CDN 非问题，自托管 P0 取消。
- §2 已加的系统宋体回退栈（`Songti SC`/`SimSun`）保留作**优雅降级**：CDN 偶发慢/失败时标题落到系统宋体，不崩美术。

### 科技树视觉统一（原 P1，已决定并落地）
- **技术美术决策：方案 A（宣纸同源）。** 理由：一个游戏一套视觉语言；原深色"开发面板"皮肤与主界面水墨体系脱钩，破坏沉浸感。
- **已实施**（tech_tree_ui.html）：`:root` token 整体改为宣纸/墨/朱红/金/玉体系；宋体字体；分支色统一为温和大地色（农绿/兵朱/工青/商金/文紫/医玉/理蓝/法琥珀/术兰）；残留硬编码冷红 `rgba(255,107,107)` 改为朱红 `rgba(139,58,58)`。仅改样式，不动逻辑。

### P2 — 暗色模式 / 弱光舒适
- 加 `prefers-color-scheme:dark` 或设置内切换，复用现有 token 反转。

---

## 4. Web 技术美术预算规格（类比资产预算）

| 预算项 | 规格 | 状态 |
|--------|------|------|
| 水墨粒子 DOM 合成层上限 | 桌面 ≤12 / 移动 ≤6 | ✅ 已落地 |
| 粒子动画属性 | 仅 `transform`+`opacity`（合成器层，不触发 layout/paint） | ✅ 已满足；新增 `will-change` |
| 过渡属性 | 禁 `transition:all`；仅列必要属性 | ✅ 已落地（残留 0） |
| Canvas 位图密度 | DPR 封顶 2x；CSS 尺寸与 backing store 分离 | ✅ 转场 + 终局曲线均已满足 |
| 文字对比度 | 正文 ≥4.5:1(AA)，大字 ≥3:1 | ✅ 已由 `tools/contrast_audit.js` 全量校验（28 项配对全过） |
| 动效时长 | 微交互 ≤150ms，场景切换 ≤400ms | ✅ 现有 token 符合 |
| 外部资源请求 | 在线可用即可；CDN 失败优雅降级 | ✅ 已满足 |

---

## 5. 验证指标 / KPI

| 指标 | 达标线 | 当前 |
|------|--------|------|
| 字体渲染 | 联网正常；CDN 失败回退系统宋体 | ✅ 已满足 |
| 粒子合成层数 | ≤ 预算(12/6) | ✅ |
| 对比度审计 | 全站正文 ≥4.5:1 | ✅ 脚本已落地，28 项配对全过（tools/contrast_audit.js） |
| CDN 优雅降级 | 字体请求失败时不崩美术 | ✅ 回退栈已加 |
| 科技树击键长任务 | 无同步整网格重建 | ✅ 已防抖 |
| 科技树视觉一致 | 与主界面同源宣纸体系 | ✅ 已落地（方案 A） |

---

## 6. 下一步建议

1. ✅ **全站对比度审计脚本已完成**（`tools/contrast_audit.js`）：解析两文件 `:root` 色板，对 28 项真实 UI「文本×背景」配对算 WCAG 2.1 对比度，自动标红 <4.5:1(正文)/<3:1(UI组件)，CI 友好（不达标非零退出）。首跑发现金/强调色 9 处不达标，已压深 `--gold`/`--acc` 修复，复跑全过（EXIT=0）。
2. **科技树纸张纹理（可选）**：如需与主界面纸感完全一致，可把主界面 `body` 的 5 层渐变纹理移植到 `tech_tree_ui.html`，但密集卡片栅格下需评估噪点，建议先灰度评审再决定。
3. **暗色模式（P2）**：若后续要做弱光舒适，复用现有 token 反转即可，成本极低。

> 文档结束。回合一交付：代码层安全修复 8 项（§2）+ 科技树宣纸同源改皮（§3）；字体 P0 按"联网游戏"指示取消。回合二交付：对比度审计脚本（§6-1）+ 金/强调色达标修复（§2 新增），全站 28 项配对对比度全过。
